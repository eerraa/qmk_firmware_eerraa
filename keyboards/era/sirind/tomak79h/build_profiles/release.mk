# Release-like TOMAK79H VIA profile: it intentionally adds no diagnostics, so a
# `release` build is whatever keyboards/era/era_build_options.mk declares plus
# the board's own lines in post_rules.mk. The storage and USART settings this
# comment used to place in "the keyboard rules" are in that post_rules.mk.
