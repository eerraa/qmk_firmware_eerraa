# 부록 A — 문서별 밀도 재구성 명세 (DOCS_SYSTEM_DENSITY_SPECS)

`DOCS_SYSTEM_PLAN.md` §8(Phase 5)의 문서별 명세. 2026-08-30에 읽기 전용
감사 에이전트 7개가 트리(worktree, HEAD `ca44e73562` + NVM 미커밋 diff)를
읽고 작성했다. **줄 번호는 그 시점의 Read 도구 번호**다 — NVM 착륙 뒤
`era_host_peer_storage_contract.md`·`era_sram_residency_contract.md`·
`era_performance_gates.md`·`era_source_map.md`는 밀린다. 실행 세션은 각 문서를
다시 열어 섹션 경계를 재확인한 뒤 따른다.

판정 기호: **K** 유지(압축) · **T** 표로 전환 · **P** 포인터로 대체 · **D** 삭제.
각 문서마다 (1) 섹션 분류표, (2) 총량, (3) MUST-SURVIVE 체크리스트,
(4) 라우팅된 헤딩(글자 그대로 유지), (5) 검사기 함정, (6) 중복(정본이 다른
곳)이 있다. 감사 본문은 영어 원문 그대로다(실행 에이전트용).

## 요약

| 문서 | 현재 | 추정 | REFUSED | 특기 |
| --- | --- | --- | --- | --- |
| `manuals/era_capture_reading.md` | 1,397 | ≈640 | 1 | 잔여 = 필드 디코드 표 |
| `contracts/era_wire_contract.md` | 989 | ≈420 | 7 | L905 "six"→"seven" 정정 |
| `contracts/era_route_contract.md` | 983 | ≈310 | 6 | L251-252 포인터 정정 |
| `manuals/era_performance_gates.md` | 584 | ≈270 | 0 | NVM diff 착륙 후; Layout Gate/Checks 이름 통일 |
| `contracts/era_authority_contract.md` | 542 | ≈170 | 0→8 | 산문 거절 8건을 세 줄 형식으로 |
| `manuals/era_board_adoption.md` | 527 | ≈176 | 0→1 | |
| `manuals/era_build_options.md` | 484 | ≈260 | 3 | L198 stale 정정 |
| `contracts/era_hid_report_contract.md` | 404 | ≈103 | 5 | 본문 전체 한국어 → 영어 번역과 압축을 한 번에 |
| `contracts/era_invariants.md` | 395 | ≈120 | 0→6 | always-on; 불변식 = 한 행(불변식·강제 수단·위치) |
| `contracts/era_host_peer_storage_contract.md` | 610(worktree) | ≈310 | 0 | NVM 착륙 뒤 재확인; H7S 봉투는 글자 그대로 |
| `contracts/era_sram_residency_contract.md` | 383 | ≈170 | 1 | REFUSED는 storage 계약의 rotation 규칙 곁으로 이동(3중복 해소) |
| `contracts/era_closed_surface_contract.md` | 273 | ≈111 | 0(영구 폐쇄 3) | **Storage Lane Boundary**는 유일 소유 |
| 나머지 9편 | 1,913 | 일반 규칙 | | `era_walkthrough.md`는 서사 유지 |

공통 검사기 사실(모든 감사가 확인): 문단 = 빈 줄로 나뉜 블록이므로 **표 전체가
문단 하나**; 주장 동사 목록 = returns/calls/sets/clears/holds/reads/writes/
advances/gates/excludes/raises/counts/runs/fires/owns; 파일 토큰은
`.c/.h/.mk/.py/.ld/.json`(`.S`·`.sh`·`.md`·`.txt`는 아님); 라우팅 형식은
`` `x.md`'s **H** `` / `` `x.md`, **H** `` / `` `x.md` **H** `` / ``(`x.md`, H)``이며
대상은 헤딩 또는 **굵은 lead-in**; 상수 검사는 같은 줄에서 이름 뒤 90자 안의
정수만 본다(값을 이름 앞에 쓰면 검사되지 않는다 — 표는 "`NAME` 값" 순서로).

---
## A1. `manuals/era_capture_reading.md` (1,397 lines, worktree == HEAD)

Checker state today: `python keyboards/era/common/tools/era_doc_refs.py` exits 0 with zero findings for this file (19 stated constant values checked across the layer). Every path the document cites resolves (incl. `scheduler/era_split_transport_scheduler_responder.c`, `keyboards/era/sirind/common/tomak_common.h`); `era_split_link.h` **Reconciliation** exists.

### Section classification

| # | Heading | Lines | n | Verdict | What survives | After |
|---|---|---|---|---|---|---|
| 1 | title + header + preamble | 1-19 | 19 | K 60 / D 40 | header lines (now two after Phase 3); one line splitting ownership with `era_identifier_map.md` / `era_performance_gates.md`; drop 16-18 meta | 8 |
| 2 | How A Capture Is Sampled | 20-21 | 2 | K | heading | 1 |
| 3 | Line pacing and sample skew | 22-48 | 27 | K 50 / T 20 / D 30 | 150 ms pacing (`ERA_SPLIT_WIRE_DIAGNOSTICS_LINE_INTERVAL_MS`); table: line family x sample instant (frozen vs `ccore..crsp` re-sampled at `index x 150 ms`, block `N..N+5`, N = enum in `split/diagnostics/era_split_wire_diagnostics.c`); rule: never delta across the two classes; drop 36-42 advice | 9 |
| 4 | Cumulative counters are totals, not rates | 49-74 | 26 | K 70 / D 30 | free-running list; rate = delta counter / delta `last` never / `raw_us`; delta raw / delta s must reproduce `scan_hz`; `raw_us` = reboot detector; `core_free` = `ram0_free_bytes` - (8 or 4), record per image set | 10 |
| 5 | Fields that do not answer the question asked | 75-105 | 31 | T 80 / K 10 / D 10 | 6-row table field x does-not-answer x read-instead; rig naming rule (102-104); drop 77-78 | 10 |
| 6 | The capture is taken by pressing a key | 106-131 | 26 | K 60 / P 20 / D 20 | keycodes + file, unbound in default/via; bind-temporarily rule; latch order -> first press after silence is a valid pre-trigger reading (`quantum/keyboard.c`); capture keycode is a settled-capture trigger -> `open` moves, `xfer` is the discriminator | 8 |
| 7 | Role-era lines are deltas, not totals | 132-181 | 50 | K 50 / P 25 / D 25 | reset per capture (function + file); role-scoped; `meas=0` = exact zero; initiator `meas=0` ordinary under DH poll -> read responder `dh era io`, `bem` corroborates; judge poll counters vs route-contract periods; `stor`/`rt` unaffected; `mode=`=last_mode -> read `rel=` on `wire pc`; 152-161 mirror mechanism -> P | 16 |
| 8 | The live CLEAN phase line | 182-209 | 28 | T 70 / K 20 / D 10 | field table (t/i/l/lp/pp/pa/ap 0-2/dl/cq x8/hs x6); event-id table 1-10; healthy ordering + event 5 terminal; 194-197 acceptance -> pointer to gates | 18 |
| 9 | The Relation's Own Counters | 210-289 | 80 | K 45 / T 25 / P 20 / D 10 | line format; `io/stor/rt` meanings, `rx` before policy gate; no periodic `SESSION_STATUS`; `csess sub` = stop edges, read beside initiator failure counters; `sfg` = forget (call site + file); `io` not a silence instrument; `rt` = sections not polls, net of 60 s anchor (`sec` 0x80); table: direction x section set counted; no defer field | 30 |
| 10 | Scheduler And Storage Lines | 290-470 | 181 | T 50 / K 30 / P 10 / D 10 | field table for the five lines; `st` 0/7/9 + `dom=255` + responder-never-0; `close+abort==open` initiator-only; sweep-shape table (all-pull 14/7/7 vs 7/7/0, all-push 0/7 + retry 7); `news` 1..7F fwd-only, `newsn`==0, `news` != `chg`; `sec=` rule (flags only at `rsp=2/9`, pure mask, `08` lock-only); `df` bits + 0x04 coincidence; `pnews` equality-only; `recency` field table; `arb` bit table (live 01/02/04, retired 08/10/20, `%u/%02X`, off-role release); queues drained; signature not tabulated. 332-338, 380-397 -> pointers | 75 |
| 11 | The EEPROM SYNC indicator shim | 471-578 | 108 | T 45 / K 35 / P 10 / D 10 | `ind`/`led`/`brk`/`slp` field tables (state bits 0-2, `brk count == rn - spans`); 160 ms floor; macro upload visible before durable (1 line); domain-blind (1 line, drop the replaced-line story); `rn` vs `spans` 3-row reading; lamp = one per operator action; lamp follows unfinished pair work; `excl=0` through apply + cause-bracket reading; 519-525 TOMAK render wake -> P | 45 |
| 12 | The storage core and its cause timeline | 579-737 | 159 | T 65 / K 25 / P 5 / D 5 | `core`/`gen`/`last`/`flast`/`qctx`/`psvc`/`span`/`fid` decode tables (all id enumerations); 25000/45000/85000 us -> derivation line; cause selector, `ev` nibble table 1-F + detail table; sampling 1/16, 32-entry ring, 16-bit ms; edge: DH and HP-promotion procedures as numbered steps, `wr/at/gap/oq` table, `ev` entry format + events 1-11 + arms 01-80 + `rs` split, 64 events/65535 ms; `via macro` field table + blink-order reading | 85 |
| 13 | The qwin Window | 738-833 | 96 | T 55 / K 35 / D 10 | field table (pc...seg, settle, smp, ovr, rearm ~93 min, fd1 bits); `scan_hz` != `smp_hz`; `sp` was `hbsp`; park credited to ending window, 60 s first-byte window, sleep share only where parks short; two device shapes (1 line each); ccore relation table (keep, drop 794-795 periods); reactive step 65.5 s; key-quiet understates reactive fix (+14.1 vs +21.7 %) | 45 |
| 14 | The `qwin_phase` rung | 834-931 | 98 | K 45 / T 30 / P 15 / D 10 | price ~4 us/pass measured per sitting; `ph/us` -> pointer to `system/era_pass_phase_diagnostics.h` + walkthrough table; divide by `ph`, millions of passes; `pmax/mx` absolutes; `over` bands 50/100/200; band owner = authority body 10 ms, ~100/s both halves, ~154 us HP HOST; LEFT/RIGHT gap = band edge -> read `hkmx` middle + >=50; `hk`/`rgb` tables; gated passes = `ph` - 4 arm counts | 40 |
| 15 | The Key Path And The Scan Facts | 932-960 | 29 | K 50 / T 30 / P 20 | histogram edges -> name `era_host_peer_matrix_link.c`; 402 us centre; first half only on key path; read histogram not mean; `wire qmk` field table | 12 |
| 16 | Scheduler State And Maintenance | 961-1014 | 54 | T 60 / K 35 / D 5 | field table; `be` init-role 1/2 latch + inverted = wedge predictor; `maint` column table (trap columns `init`, `rsp`; `entry-hkwork`); `rsnp`; `due` bit table live/retired; `open_ms` boot-only, 65535 | 30 |
| 17 | Runtime section counters | 1015-1061 | 47 | K 50 / T 35 / D 15 | `sect` = cumulative twin of `rt`, HP reads here; `app=` counts distinct applied values, producers table (rgb 2, vis 2, auth 1); three-way reading table (sent-not-applied / didn't-send / applied); `lay`; drop 1036-1039 pre-correction history | 22 |
| 18 | The shared clock and the time anchor | 1062-1121 | 60 | K 60 / T 25 / D 15 | `sync`/`anc`/`corr`/`ahold` table; applies+corr per arrival vs back per apply; `back` = truncation counter; first apply = offset adoption (`applies=1 interval_ms=0`); sender = responder; subtract pacing distance; no numeric pass condition on `back_max_ms` | 24 |
| 19 | Tap-hold activity | 1122-1146 | 25 | T 90 / K 10 | field table + cross-half reading table | 14 |
| 20 | Reset Edge | 1147-1154 | 8 | K | `dtap_ms`, `rel/prev`; `hbmiss` retired | 4 |
| 21 | Relation-Role Lines | 1155-1199 | 45 | T 55 / K 35 / D 10 | `resp` fields + structural final-profile row; host/peer field tables; no `hb`/`chb` (1 line); `vis`/`rgb` distinct applied; PEER line unreachable -> read `sect rx`/`app=`/HOST tx | 22 |
| 22 | Communication-Core Lines | 1200-1201 | 2 | K | heading | 1 |
| 23 | Lifecycle - `wire ccore` | 1202-1268 | 67 | T 55 / K 40 / D 5 | field table; REFUSED block verbatim; `att` rollback; `stop` spelling; `err` stage/phase tables; `park/idleus` deltas only; `cap/dead` structural zero, `cap=1` -> `LOCAL_NO_LINK`; streak clear = service report, not `start()`; zero proves nothing on a wedged half (fast path) | 38 |
| 24 | Queue - `wire cqueue` | 1269-1273 | 5 | T | field row | 3 |
| 25 | Owner - `wire cown` | 1274-1290 | 17 | T | field table, `own` 1 retired, `io` order | 12 |
| 26 | Session - `wire csess` | 1291-1300 | 10 | T | field table | 8 |
| 27 | Responder - `wire crsp` | 1301-1361 | 61 | T 50 / K 40 / D 10 | field table (`src` 1 retired, no `clr`); `arx`/`undec` definitions (step rule -> pointer); `quiet = prep - resv`; `resv == acc`; high quiet/low acc = design, folded counters one pass stale; request-mask bits, 0x01 != HP | 32 |
| 28 | Source-push - `wire csp` | 1362-1397 | 36 | T 60 / K 40 | lane = matrix only (`_CORE0_LANE_SECTIONS`); field table; five HSRSP fields must read zero; `rxm` 0/1 | 20 |

**Totals: 1,397 -> ~640 lines** (~54 % cut; the residue is field tables, which is what a decode manual is).

### MUST-SURVIVE checklist
- REFUSED (l.1206): "delete the six `av=` fields and `rxm=` as dead weight." - all three lines verbatim.
- Constants (tree-verified): `ERA_SPLIT_WIRE_DIAGNOSTICS_LINE_INTERVAL_MS` 150; `ERA_SPLIT_RESPONDER_FIRST_BYTE_TIMEOUT_MS` 60 s (60000U); `ERA_SPLIT_QWIN_SEGMENT_MS` 10 s; `ERA_SPLIT_QWIN_SETTLE_MS` 0; `ERA_SPLIT_AUTHORITY_POLL_PERIOD_MS` = SOF_FRESH (10); `ERA_SPLIT_WIRE_DIAGNOSTICS_SCHEDULER_LINES` = N, deliberately unstated (tree: 22); `ERA_HOST_PEER_STORAGE_CAUSE_TIMELINE_ENABLE=yes` / `ERA_SPLIT_QWIN_COUNT_ONLY_ENABLE=yes` (`build_variants/*.mk`); `ERA_SPLIT_WIRE_HOST_PEER_SOURCE_PUSH_CORE0_LANE_SECTIONS` = SECTION_MATRIX; `ERA_SPLIT_WIRE_SECTION_ELIGIBLE_DUAL_HOST_RSP` omits LOCK.
- Bit layouts: `sec=` flags reserved 0x04/0x08/0x20; section mask 0x01 layer, 0x02 rsp ACTIVITY, 0x04 AUTHORITY, 0x08 lock, 0x80 anchor, push ACTIVITY 0x10; `df` FIELD 0x01 EEPROM/0x02 INPUT/0x04 RGB (0x04 also the macro domain in `chg/probe/psh/cfl`); `arb` 0x01/0x02/0x04 live, 0x08/0x10/0x20 retired, `arb=%u/%02X`; `due` 0x01 attach, 0x04 HP push, 0x10 publication, retired 0x02/0x08/0x20/0x40; `brk state` bit0/1/2; `fd1` bits 3..0; edge `arms` 01/02/04/08/10/20/40/80, `rs>>5` role, `rs&1F` state; `crsp last` request mask 0x01/0x02/0x04.
- Enumerations: `st` 0 IDLE / 7 HOST_READY / 9 HOST_CLOSED, `dom=255`; modes 3/4/0; probe stages 0-7; result 0-4; failure 0-12; class 0-10; access 0-4; 255 absent; `ddu`/`span` sentinel -2147483648, `qctx` 4294967295; `fid detail` 0-3, `op=EE`; `psvc kind=1`, reason 3/4; cause events 1-F + low-nibble details; edge events 1-11; CLEAN events 1-10, `ap` 0/1/2; `err` stage 0-5 {0,0,1,vector_table,stack_end,entry}, phase 0-3; `own` 0/2 (1 retired); `role` 0/1/2; `src` 0/2 (1 retired); `rxm` 0/1; `maint` order entry/stor/rsp/stand/init/time/mode/route; twelve segments RAW...REST; `resp` final profile th0 svc0 blk1 io0 trn0 rst0 qsc1.
- Fixed figures: 150 ms pacing, block `N..N+5`; 60 s anchor refresh (`ERA_SPLIT_TIME_ANCHOR_REFRESH_MS` 60000 - name it); 160 ms indicator floor (`ERA_SPLIT_EEPROM_SYNC_MIN_VISIBLE_MS` - name it); 1000 ms dirty-quiet; 1/16 chunk sampling, 32-entry ring, 16-bit ms, byte saturates 254; 64 edge events, 65535 ms; `open_ms` 65535; keypath edges 320/384/448/512/640/896/1408 (`era_host_peer_matrix_link.c:54`); `over` 50/100/200 (`ERA_PASS_PHASE_BAND1..3_US` - name them); 25000/45000/85000 us = 20 ms (`ERA_SPLIT_PEER_RESPONSE_WINDOW_MS`) x scale + 5 ms; `news` 1..7F; sweep shapes 14/7/7 vs 7/7/0 and 0/7 (+retry 7); rearm ~93 min; hit tracker UINT16_MAX = 65.5 s; `core_free` offsets 8 and 4.
- Owner decisions: bind a diagnostic keycode temporarily and restore (113-115); name a rig by relation, never shorthand (102-104); no periodic `SESSION_STATUS` (223-225); no defer field on `rt` (285-287); no expected table for the relation-open signature (465-469); no numeric pass condition on `back_max_ms` (1070-1071); lane-sections constant named not spelled (1367-1370); `stop` spelling kept (1221-1224).
- Device-established facts (one line each, with what they ground): `sec=` as image identity cost a gate run -> never (378); `arb=16/00` misread -> decimal/hex (445); `cfl=00` misread -> queues drained (461-462); signature mis-scoped once -> same role+relation only (466-469); forced core1 kill wedged only the two inverted `be` combos -> read the pair (971-974); `start()` true yet core1 died at first pass -> streak clear on service report (1253-1258); two parks of 29,999,997 us per 60 s window; idle no-link `loop` +1-2/min at `idle=0` (772-778); ccore rates 100 / 2,955+1,970 / 6 per s (786-788); ~100 passes/s >50 us both halves, tens us vs ~154 us (882-890); seventy-fold LEFT/RIGHT misread -> `hkmx` rule (892-896); 402 us source-push (938); +14.1 % vs +21.7 % (828-829); ~4 us/pass rung price (839); responder outliving initiator > poll+housekeeping = mirror finding (494-496); sitting labels have been wrong -> ccore decides relation (780-782).
- Headline rules: never delta across sample classes (47); rate = delta/delta `last`, never `raw_us` (58-63); delta raw / delta s reproduces `scan_hz` (63-65); never difference `_era` lines (142); read `rel=` on `wire pc` (178-179); first press after silence valid (122-123); `xfer` not `open` (129-130); `io` rising on idle DH = mode working (254-259); `rt` from a settled window (268-271); cross-half tx/rx != loss (273-274); `csess sub` vs initiator failure deltas (235-238); responder never `st=0` (303-305); `proof` counts `PROOF_RSP` (318-319); `newsn`==0 (357); `pnews` equality only (414); responder `arb` 0x02/0x04 or lit `due=` = finding (456-457); lamp one per action (552), follows unfinished work (561); `excl=0` through apply (568); HP edge leg = promotion route, never reboot the promoted half (667-680); `cq`/`hs` frozen from event 4, event 5 terminal (195-197, 207-208); sleep share only where parks short (768); divide by `ph` (857); `pmax` absolutes (862-865); read histogram not mean (950); sends from `rt`/`atx` never `adv` (1055-1057); subtract pacing distance (1113-1114); `wire sect rx` not `rt` in HP (1197-1198); never `resv - acc` (1338); five csp HSRSP fields zero (1391); mask bit 0x01 != HP matrix (1353-1355).

### ROUTED HEADINGS
No inbound route uses the checker's checked forms - zero headings are mechanically pinned. Routes are by field name, so keep these as findable entries: `wire csp` entry (`era_host_peer_matrix_contract.md:102`), `sec=` rules (`era_wire_contract.md:646`), `ahold` (`era_wire_contract.md:824`), `news`/`pnews`/`arb`/`recency` (`era_active_index.md:99`), "the counters" (index:108, `.claude/rules/era-matrix-hsrsp.md:21`), gated passes (`era_source_map.md:185`), `qwin_phase` segments (`era_walkthrough.md:29`), cause-timeline status decode (`era_identifier_map.md:81`), how to bind and read `WIRE_DIAG` (`era_build_and_flash.md:174`), rate off a counter and `last` (`era_route_contract.md:917`), poll counter vs period reciprocal (`era_route_contract.md:179,242`). Outbound from this file: **Capacity And Publication** (341-342, exists), **USB Session** (450-451, exists), `era_split_link.h` **Reconciliation** (1324, exists) - all unchecked forms today; joining 341-342 onto one line arms `SECTION_PAREN` and it resolves.

### CHECKER TRAPS
- Function-with-verb, no file in the paragraph: **1004-1012** (`timer_init()`, `keyboard_init()` "does", `split_pre_init()` "runs") - passes today only because the whole bullet list 963-1013 is one paragraph containing `era_split_transport_scheduler.h`/`era_split_scheduler_events.h`; splitting it into rows trips `check_claims` - name `quantum/keyboard.c`. Same shelter covers **1221-1224** (`..._request_quiesce()` "parks" - verb not in the checker's list, but the rule wants `split/communication_core/era_split_communication_core_lifecycle_rp2040.c`) and **493-500** (`era_host_peer_storage_indicator_pending()`, no listed verb; add `split/era_host_peer_storage.c`). Bare basenames at 138, 232, 296, 440, 980, 997, 1340 pass `CLAIM_FILEREF`; prefer common-layer-relative paths.
- Constant pairing: values written *before* the name (25, 766, 796, 886) are unchecked today; writing name-then-value in a table arms the check and every one passes (150, 60 s, 0, 10). Do not put arithmetic like `N+5` on the same line as a define name.
- `2026-` dates: **386** (`2026-08-19`) - delete (report/history; `era_wire_contract.md:174` owns it).

### Duplication (canonical owner elsewhere)
1. 113-115 bind/restore rule - `era_performance_gates.md` **The Instruments Are In The Tree And Off In Release** (38-41), same sentence; one must become a pointer.
2. 194-197 CLEAN acceptance - gates **EEPROM CLEAN agreement device gate** (186-190).
3. 380-387 `SESSION_STATUS` reserved-zero + date - `era_wire_contract.md` **`SESSION_STATUS`** (168-180).
4. 392-397 section-mask values - `era_wire_contract.md` **`HOST_PEER_HOST_SOURCE_RSP`** / `era_split_wire_protocol.h:232-239` (the doc says so itself).
5. 359-363 `news` forward-only - storage contract **Storage News Value And Relation-Open Audit** (319-322), `era_overview.md` Glossary (132), wire contract body (756).
6. 407-412 `pnews` value bits/bit7 - wire contract STORAGE_NEWS body (770-773) already states `news=`/`pnews=` print value bits, `mir` is the flag.
7. 418-430 recency rules - storage contract **Recency Layer** (already pointed; keep decode only).
8. 89-91 and 398-406 `req=` EEPROM-only / `df=` change log - `era_identifier_map.md` **USB Session** (270-276).
9. 794-795 and 883-886 poll periods 10 ms / 1 ms - `era_route_contract.md` (the doc's own 165-168 says no second table may exist).
10. 1317-1332 `undec`/`arx` step rule - route contract **SESSION_STATUS Discovery And Liveness** (630-642) + `era_split_link.h` **Reconciliation**.
11. 844-855 twelve segments - `era_walkthrough.md` **1. One pass of the keyboard loop** table (31-50); 862-871 and 919-930 - `era_source_map.md` **Diagnostics** row 185 restates absolutes/at-most-one-arm (that row points here, so shrink the map row, not this).
12. 1177-1184 heartbeat retirement - `era_identifier_map.md` **Routes And Route Reasons** (40-43) / **Payloads** (99).
13. 227-238 stop -> revalidation mechanism - route contract **The standing exchange grant** (436-444).
14. 332-338 no-rollback apply - storage contract **Replacement Apply: ADMIT And Public Authority**; 339-345 - **Capacity And Publication**; 484-491 - **Dynamic Macro Transaction**.
15. 180, 213-214 mode ids - `era_identifier_map.md` **Modes** delegates values to `era_split_mode_planner.h`.

Not source-verified (checker does not reach them; keep verbatim): `arb` enum comment-vs-name claim (437-442), cause/edge event id lists, probe/failure/class enumerations, policy FIELD bit values.

---
## A2. `contracts/era_wire_contract.md` (989 lines)

Source spot-checks (via `git grep`/`git show HEAD:` on `split/era_split_wire_protocol.h`, `split/era_split_eeprom_sync.h`, `split/era_host_peer_storage.h`) confirm every marker, body size, mask, op id and cadence the document states. One stale sentence found: L905 says DUAL-HOST carries "six push sections"; `ERA_SPLIT_WIRE_SECTION_ELIGIBLE_DUAL_HOST_PUSH` (protocol.h:592) has seven members (RESTART_ARM was added after the sentence). Fix to "seven and seven".

### Section classification

| Heading / sub-topic | Lines | N | Verdict | What survives | After |
|---|---|---|---|---|---|
| Title + header block | 1-7 | 7 | K | header (two lines after Phase 3) | 5 |
| `## Compact Payload Kinds` | 8-19 | 12 | K | the table; trim status prose | 10 |
| `## HOST-PEER Class` | 20-46 | 27 | K 50 / P 25 / D 25 | id block 22-29; L39 MUST NOT; L35-36 owner decision (rename declined) as one line; L41-45 -> one line naming `split/era_split_wire_payload.c` | 14 |
| `## HOST-PEER Replacement Storage Class` | 47-137 | 91 | T 45 / K 35 / P 10 / D 10 | op table 52-71 as-is; rules 75-92, 102-114, 120-128 as a rule table (sizes, prefix, bytes 8..11 zero, CRC-hint bytes 12..14, sole alternate op, response-slot rule); L93-101 -> one line + pointer; L115-119 one line each; L129-136 -> one row naming the classifier file | 45 |
| `## SESSION_STATUS` | 138-193 | 56 | K 50 / T 20 / D 30 | fields 140-142; flags block 144-151; validation as 3-row table (153-166); L168-179 essay -> 2 lines (each carrier refuses what it has no fact for; new bit = one line in the MASK, whole-frame reject of older halves); L181-185 capture-dating rules 2 lines; L187-192 one pointer line | 28 |
| `## HOST_PEER_SOURCE_PUSH` - envelope + section table | 196-216 | 21 | K/T | keep; add per-relation eligibility columns (values verified at protocol.h:538/592) | 16 |
| - STORAGE_PENDING body + narrative | 218-245 | 28 | K 30 / P 20 / D 50 | body block; forced-first-cross rule; initiator copy vs responder bit7; "summary/news do not stand in" one line; lamp-bridge origin story deleted | 10 |
| - RESTART_ARM body, table, CLEAN ordering | 247-328 | 82 | K 50 / T 20 / P 15 / D 15 | body block; validation table 276-281 + malformed row; responder rides AUTHORITY (2 lines); "carries any act, acts in `split/era_split_restart_agreement.h`" 1 line; PREPARE->COMMIT ordering 3 lines; absolute-deadline rule 1 line; forced-cross/rotation rules 3 lines; budget arithmetic 320-328 folded into deferral table | 32 |
| - VISUAL / RGB_STATE push twins | 330-342 | 13 | K | 2 lines each | 4 |
| - MATRIX+AUTHORITY never one frame | 344-354 | 11 | K | 2 lines (arithmetic + separate routes) | 2 |
| - Rules (incl. REFUSED L362-366) | 356-401 | 46 | K 60 / T 40 | REFUSED verbatim; mask-walk MUST; zero-mask rule; eligibility-table enforcement; closed cells (RGB/INPUT/ACTIVITY in HOST-PEER, matrix forms, EEPROM DATA) as a table with one-phrase reason + assert line (protocol.h:604-626) | 22 |
| `## HOST_PEER_HOST_SOURCE_RSP` - envelope + section table | 405-428 | 24 | K/T | merge with push table into one master table (marker x bytes x direction x relation) | 14 |
| - shared discipline, lock-only reason, new-marker rule, no-section/visual+RGB | 430-461 | 32 | K | 3 + 1 + 1 + 2 lines | 7 |
| - lock, INPUT, AUTHORITY bodies | 463-495 | 33 | K | verbatim blocks | 26 |
| - AUTHORITY rules (497-518) | 497-518 | 22 | K 60 / D 40 | MUST leave `bulk_page_supported` untouched; shared validation rules; mask `0xFF` asserted (protocol.h:456) - 4 lines | 4 |
| - three REFUSED blocks | 520-536 | 17 | K | verbatim | 17 |
| - "bits 3..7 are here because..." | 538-545 | 8 | D | conclusion is the REFUSED blocks + `era_closed_surface_contract.md` L155-156 | 1 |
| - AUTHORITY restart validation table + tuple rules | 547-570 | 24 | T/K | table 550-557 + malformed row + "full tuple in shadow/edge; relation identity part of acceptance; REQUEST before either qualified state" 3 lines | 13 |
| - two consumers differ; 16-bit values; lets SESSION_STATUS stop | 572-589 | 18 | K 40 / P 60 | 2 lines naming **both** files (see traps); 1 line; 1 pointer line | 4 |
| - INPUT layer width | 591-599 | 9 | K/P | 2 lines (`era_split_peer_layer.c` assert; every value valid) | 2 |
| - ACTIVITY body | 601-611 | 11 | K | verbatim | 9 |
| - ACTIVITY semantics, counters vs stamps, marker reuse | 613-647 | 35 | K 40 / D 60 | window flag derived from `era_tapping` bridge; fields live only while peer window up; timestamps are the judgment, counters dedup; marker `0x02` reuse = owner decision (1 line, capture-era rule -> `era_capture_reading.md`); the 16-bit rejection is already REFUSED L520 | 8 |
| - three owner decisions + three REFUSED + two approximations | 649-679 | 31 | K | 3 lines + 16 verbatim + 2 lines | 22 |
| - visual-resync body + rules | 681-713 | 33 | K 70 / D 30 | body block (reasons 1/3 "(no sender)" already annotated); no reason-only form + pre-2026-08-10 dating (1 line); validator deliberately not narrowed (1 line) | 18 |
| - RGB body + sleep rules | 715-750 | 36 | K 50 / P 50 | body block; sleep bit never crosses in DUAL-HOST (1); apply never writes render gate -> pointer to `era_authority_contract.md` **Lighting Sleep Ownership** (1); captured bit = resolved decision (1); both-halves-dark placement (1) | 17 |
| - storage news body + naming | 752-778 | 27 | K 60 / D 40 | body block; constant names + `_FLAG_PENDING 0x80` dating rule (2026-08-14) 2 lines; `SETTLED_DIRTY_MASK` one-site note 1 line | 13 |
| - time-anchor body, encode order, valid lengths | 780-806 | 27 | K 60 / D 40 | body block; encode = ascending bit, priority != encode for ACTIVITY (1); "3+8+4 = 15" (1); RGB/visual no legal len already stated above | 9 |
| - Rules: slot-only, no independent send | 810-813 | 4 | K | 2 lines | 2 |
| - time-anchor service + correction | 814-829 | 16 | K 40 / P 60 | responder = time authority both relations; both ends corrected (`ahold` -> `era_capture_reading.md`); once per open/reopen then slow bounded cadence | 4 |
| - storage news rules | 830-877 | 48 | K 30 / D 50 / P 20 | "ask, names nothing" (1); zero legal + forced nonzero refresh on `ERA_HOST_PEER_STORAGE_DIRTY_QUIET_MS` (1); gate `value != 0` boot-scoped (1); forced cross per open incl. zero + device fact (2); bounded repeat + owner open question (1); one-carrier pointer (1) | 7 |
| - no-section answer, ACK under exclusivity, MUST NOT plan ineligible | 878-901 | 24 | K | 1 + 2 + 2 lines (device-measured storm kept as one clause) | 5 |
| - INPUT DUAL-HOST only + silence legs; AUTHORITY both + edge-consumed | 902-920 | 19 | K 60 / P 40 | 2 + 2 lines; fix "six" -> "seven" | 4 |
| - deferral order, per-relation rejection, `_Static_assert` family | 922-971 | 50 | T 70 / K 30 | order as a table (core / yielding rank / anchor); per-relation-order rejection as a REFUSED block (3 lines); assert family as a table (never-deferring core x2 relations + authority-only; drain per deferrable; arm beside AUTHORITY = 15; AUTHORITY+RGB 14 > 12 not asserted) | 16 |
| - PEER accept rule, RGB apply scope, inbound rejected | 972-980 | 9 | K | 4 lines | 4 |
| `## Control-Only Payloads` | 982-989 | 8 | K | 3 bullets | 5 |

**Totals: 989 -> ~420 lines** (six verbatim REFUSED blocks ~34 lines, 10 body blocks ~95, tables ~90, rules ~200).

### MUST-SURVIVE checklist

REFUSED blocks (all three lines each):
- L362 "restore the layout walk's variable-length machinery."
- L520 "widen the response section mask to 16 bits to free a marker."
- L528 "retire a response section to free its marker."
- L532 "widen the AUTHORITY body to eight bytes for a spare flags byte."
- L658 "symmetric event replication, or a both-hosted composed relation, ..."
- L664 "an independent cross-half kill-switch for the family."
- L669 "cross-half chords - S3, the ladder's third rung."

Fixed data (all verified against source):
- `HOST_PEER_CLASS 0x20`; `0x20` SOURCE_PUSH, `0x21` HOST_SOURCE_RSP, `0x22` unassigned, `0x23..0x2F` reserved; class `0x6x` carries no reservation (L22-37)
- `EEPROM_SYNC_CLASS 0xE0`; ops `0xE0..0xEE` as at L55-69, `0xEF` reserved (`split/era_split_eeprom_sync.h:28-48`)
- compact storage body = 15 bytes; prefix control/op/domain/schema/16-bit generation (L75-77); SYNC_STATUS domain `0xFF` = summary; summary RSP bytes 8..11 permanently zero (L78-85); PUSH_CTL phase in byte 6 (L89); CHUNK_REQ 24-bit CRC hint bytes 12..14 (L107)
- bulk: payload <= 264, frame <= 271, data 0..252 (`CHUNK_RSP`) / 1..252 (`PUSH_CHUNK_REQ`), no zero-length push form (L86-88, 102-107)
- SESSION_STATUS: `payload_len == 9`, byte1 `0x10`; flags `0x01/0x02/0x10/0x40/0x80` assigned, `0x04/0x08/0x20` reserved-and-rejected; `0x20` (was `dual_host_ready`) never reassigned; role = exactly one of `0x03`; `0x40` only with `0x02` (L144-166, 181-185)
- push markers/bytes: `0x01` MATRIX `ERA_SPLIT_WIRE_HALF_MATRIX_BYTES` (7 nine-col / 9 `sirind/tomak`), `0x02` INPUT_LAYER 1, `0x04` AUTHORITY 7, `0x08` RGB_STATE 7, `0x10` ACTIVITY 11, `0x20` VISUAL 8, `0x40` STORAGE_PENDING 1, `0x80` RESTART_ARM 5 (L207-216)
- response markers/bytes: `0x01` INPUT 1, `0x02` ACTIVITY 11 (reused), `0x04` AUTHORITY 7, `0x08` LOCK 1, `0x10` VISUAL_RESYNC 8, `0x20` RGB 7, `0x40` STORAGE_NEWS 1, `0x80` TIME_ANCHOR 4 (L419-428)
- eligibility: HP push = MATRIX|AUTHORITY|STORAGE_PENDING|RESTART_ARM; DH push = INPUT|AUTHORITY|RGB|ACTIVITY|VISUAL|STORAGE_PENDING|RESTART_ARM (7); HP rsp = AUTHORITY|LOCK|VISUAL|RGB|NEWS|ANCHOR; DH rsp = INPUT|ACTIVITY|AUTHORITY|VISUAL|NEWS|RGB|ANCHOR (7)
- STORAGE_PENDING body bit0 (`_FLAG_PENDING 0x01`), bits 1..7 reserved-refused (L220-226)
- RESTART_ARM body: bits0..1 param (`0x03`), bits2..3 act (`0x0C`), bits4..7 refused; bytes1..4 `T_commit` LE sync-timer ms; act 0 idle / 1 link / 2 CLEAN / 3 refused; table L276-281; CLEAN param 0/3 malformed, `param_max` 0 (L249-286)
- AUTHORITY body: flags bit0 `0x01` host_open, bit1 `0x02` no_host, bit2 `0x04` matrix_ready, bits3..4 `0x18` param, bits5..6 `0x60` act, bit7 `0x80` qualified; mask `0xFF` asserted; bytes1..2 usb_epoch, 3..4 host_open_gen, 5..6 host_close_gen LE (L481-495); table L550-557 + malformed tuples L559-560
- lock body bits0..2 Num/Caps/Scroll (`VALUE_MASK 0x07`) (L465-470); INPUT = 8-bit `layer_state`, every value valid (L472-477, 591-599)
- ACTIVITY body layout L604-611 (bit0 window open; counters mod 256; two LE ms stamps)
- visual-resync: reason 0 RELATION_OPEN, 1 TX_OVERFLOW (no sender), 2 TICK_GAP, 3 RELATION_REOPEN (no sender), 4 RENDER_RESET = `REASON_MAX`; baseline `N = HALF_MATRIX_BYTES`, bit = `row * MATRIX_COLS + col`; no reason-only form (L683-699)
- RGB body: bit0 `0x01` enabled, bit1 `0x02` sleep, bits2..7 zero; byte1 mode bits6..7 zero; hue/sat/val/speed/LED flags (L717-729)
- storage news: bits0..6 value (`_VALUE_MASK 0x7F`, 1..127 forward-only, 0 = nothing), bit7 `_FLAG_PENDING 0x80`; no reserved bits; carrier field names L763-767; `SETTLED_DIRTY_MASK` survives only at `communication_core/era_split_communication_core_standing.h` (L754-778)
- time anchor: 4 bytes responder sync-timer ms LE; responder = time authority in both relations (L782-786, 814-816)
- budgets vs `ERA_SPLIT_WIRE_COMPACT_MAX_PAYLOAD_LEN` 15: visual+RGB 8+7 > 12 (L451-459); MATRIX+AUTHORITY 3+7+7 (L344-354); AUTHORITY+RGB 14 > 12 (L968); anchor beside visual 3+8+4 = 15 (L804-806); arm beside AUTHORITY 3+7+5 = 15 (L325-326); never-deferring cores L955-957 and L381-384; ACTIVITY beside one one-byte fact not both (L961-963)
- cadences: news refresh period = storage dirty-quiet interval (`ERA_HOST_PEER_STORAGE_DIRTY_QUIET_MS` 1000) (L842-843, L244); anchor once per open/reopen then slow bounded cadence (L825-828); 200 ms judgment window reference (L936)
- deferral order: core (INPUT, lock, news, push STORAGE_PENDING, AUTHORITY) never defers; yielding rank RESTART_ARM > ACTIVITY > visual > RGB; anchor yields to all; encode order = ascending marker bit (L788-793, 922-941)

Owner decisions (one line each): rename of the `HOST_PEER` envelope declined (L35-36); `0x20` reassignment banned (L181); ACTIVITY marker `0x02` reused (L639); DUAL-HOST no-added-hop is the product, skew accepted as physical, arming derived not owned (L649-656); visual validator deliberately not narrowed (L711-713); per-relation deferral order rejected (L943-948); storage-news repeat stop is an open question (L871-873); both-halves-dark belongs in the sleep decision, never a wire apply (L747-750).

Device-established facts (one line + what they ground): post-rotation news byte `0x00` never crossed, mirror stuck lit -> grounds forced cross incl. zero (L862-864 -> L854); anchor re-capture publish storm on a DUAL-HOST Right -> grounds MUST NOT plan ineligible sections (L898 -> L892); press+release predominantly cross in one image -> grounds timestamp-ordered judgment (L629-632 -> L624); lock-desync-in-DUAL-HOST capture named as the falsifier (L442-443).

Headline rules: L39, L83-85, L93+L101, L108-114, L158-166, L358-360, L367-370, L371-376, L388-401, L430-437, L445, L451-459, L501-502, L560-563, L697-699, L731, L738-745, L810-813, L878-899, L912-920, L922-923, L972-980.

### ROUTED HEADINGS

Ran the checker's own `SECTION`/`SECTION_PAREN` regexes (`era_doc_refs.py:108-111`; note `headings()` at :241-256 also counts every **bold lead-in** as a target) over `keyboards/era/common/docs`, `AGENTS.md`, `CLAUDE.md`, `.claude/rules`, and ERA source. **No file routes to a heading or bold lead-in of this document by name** - the only matches are `**first**` (a `ROUTING_WORDS` exclusion) at `era_active_index.md:93,108` and `.claude/rules/era-matrix-hsrsp.md:16`. Seven source comments cite the file by name only, no `:line` (`era_host_peer_responder.c:147`, `era_host_peer_response.c:372`, `era_split_wire_protocol.h:150,248,431,824,843`), so renumbering is safe. Informal prose routes that must stay findable (no exact text bound): AUTHORITY validation table (`era_authority_contract.md:165`, `era_closed_surface_contract.md:148`); marker reuse + capture ambiguity (`era_closed_surface_contract.md:74-75`); "for the reasons" the request rides AUTHORITY flags and the arm rides RESTART_ARM (`era_source_map.md:167`); eligibility columns (`era_performance_gates.md:389`); both bodies (`era_closed_surface_contract.md:124,159`).

Outbound routes this file makes that must survive verbatim (all targets verified to exist): `era_authority_contract.md`'s **Matrix Ready** (L165), **Lighting Sleep Ownership** (L741); `era_host_peer_storage_contract.md`'s **Diagnostics** (L232); `era_route_contract.md`'s **One carrier for the response section set** (L877).

### CHECKER TRAPS

- Function claims: L134 and L155 name `split/era_split_wire_payload.c` - keep the file when compressing. L573 `era_split_wire_authority_equal()` names **no file** (it is defined at `split/era_split_wire_payload.c:132`); the paragraph passes today only because "compares"/"ignores" are not in `CLAIM_VERB` and L577 names `split/era_split_scheduler_session.c` for the other function. Any rewrite using reads/holds/sets must name both files.
- Dates: L174 `2026-08-19` - report, delete (the surviving fact is "mask `0xFF`, asserted"). L699 `pre-2026-08-10` - rule-evidence, keep (capture dating). L769 `2026-08-14` - rule-evidence, keep (capture dating).
- Constant/value pairs the checker will compare if written beside a name: `COMPACT_MAX_PAYLOAD_LEN` 15, `ACTIVITY_BYTES` 11, `AUTHORITY_BYTES` 7, `RGB_STATE_BYTES` 7, `TIME_ANCHOR_BYTES` 4, `RESTART_ARM_BYTES` 5, `DIRTY_QUIET_MS` 1000 - all as stated.

### Duplication (canonical owner elsewhere)

- L49-50 + L93-101, L115-119: state machine, retry, CRC roles, mixed-revision -> `era_host_peer_storage_contract.md` **Fixed Wire Contract**/**Transaction State Machine**/**Retry, Duplicate, And Failure Semantics**; identical-image rule -> `era_source_map.md` **Stored-Data Compatibility**. Keep only the op table and sizes here.
- L227-245 STORAGE_PENDING composition/consumer -> storage contract **Diagnostics** (L580); the doc itself says it owns "the encoding above and nothing else".
- L288-318 CLEAN PREPARE/COMMIT ordering, quarantine, controlled reset -> storage contract **Why An EEPROM Clean Is An Agreed Restart** (L487) and `split/era_split_restart_agreement.[ch]`.
- L320-328 and L381-387 restate the deferral/budget statement L922-971 claims as its "single statement" - internal duplication; one table.
- L538-545 duplicates `era_closed_surface_contract.md:155-156`; L642-645 duplicates the REFUSED at L520.
- L738-750 sleep/render-gate ownership -> `era_authority_contract.md` **Lighting Sleep Ownership** (L85, L114).
- L814-829 anchor correction mechanism, `ahold` -> `era_capture_reading.md` **The shared clock and the time anchor** (L1062).
- L830-877 news value meaning/refresh/idle property -> storage contract **Storage News Value And Relation-Open Audit** (L317); L646-647 and L697-699 capture-era rules -> `era_capture_reading.md` `sec=` rules (L370-396).
- L344-354 matrix route separation -> `era_route_contract.md`/`era_host_peer_matrix_contract.md`; L585-589 and L187-192 -> `era_route_contract.md` **SESSION_STATUS Discovery And Liveness** (already pointers).

---
## A3. `contracts/era_route_contract.md` (983 lines)

Source spot-checks passed: every constant the document states equals the tree (`scheduler/era_split_transport_scheduler_internal.h`, `era_host_peer_storage.h`, `era_split_wire_protocol.h`, `era_split_link.h`, `era_split_transport_scheduler_routes.c:38`), and every `_Static_assert` it narrates exists (internal.h:167-170, timing.c:38, routes.c:56-58, scheduler.c:1246).

### 1. Section classification

| Heading | Lines | N | Verdict | What survives | After |
| --- | --- | --- | --- | --- | --- |
| header | 1-10 | 10 | K | header (two lines after Phase 3) | 6 |
| ## Common Route Priority | 11-93 | 83 | K 55% / P 25% / D 20% | 4-item priority list; "storage exclusive vs normal traffic, not vs recovery"; transaction-boundary rule + generic-ring/storage-slot mutual admission (files named); queue-freshness window = response window x scale + 5 ms -> 25/45/85 ms row; exclusive span = transfer phase; CLEAN quarantine as one admission-state row (forbids select/publish/admit/snapshot, admits SESSION_STATUS + standing exchange, only controlled reset clears); "exclusivity suppresses initiator routes never responder's answer"; heartbeat admission = relation fact, matrix admission = fact AND exclusivity. Delete the busy-HOST collapse essay (83-92 -> 2 lines) | 26 |
| ## Route Kind And Reason | 94-261 | 168 | T 45% / K 30% / D 25% | kind-vs-reason definition (2 lines); reason table (reason / wire op / trigger / selector core / C enumerator y/n - `IDLE_PROOF`/`STORAGE_ACTIVE` have none per `era_identifier_map.md:50-53`); cadence table (see checklist); rules: no post-relation `SESSION_STATUS` cadence, no core0-originated cadence in a live relation, `RESPONSE_WINDOW` is not a cadence + stop rule, new reason must define surface/ownership/cadence, DH period unconditional, period = responder latency bound, too-short period cannot fail a build + device reading (`era_capture_reading.md`). Delete 124-130 enum history, 214-242 essay except three one-liners, 170-183 reading-vs-build meta | 44 |
| ## Runtime Execution Owner | 262-285 | 24 | K 70% / D 30% | core0 never polls core1; two carriers (result = lane facts, standing state = section set); full-result-ring error path unsticks at next authority sample; route authority != execution ownership, epochs move core1 only, pointer to `era_invariants.md` for CORE1_FULL. Delete the replaced-flag shape (268-271) | 6 |
| ### The standing exchange grant | 286-454 | 169 | K 50% / T 20% / P 15% / D 15% | grant rule (one kind, two reasons, nothing else); period table DH 1 ms / HP 10 ms, DH 1/2/4 ms by level = `POLL_MS x wire_scale()` (backend owns scale, link unit answers none; HP does not scale); HP arm precondition = `matrix_ready` both directions (routed-to by matrix contract); plan-field table (epoch, generation, enable, period, push mask, bodies incl. restart phase w/ absolute commit deadline); publish-on-change; field-follows-eligibility (INPUT_LAYER); route-due bit DH-only, consumed at top of `publish_standing_plan()`, refresh is backstop (files); result latest-state; snapshot discipline + REFUSED 383-386 verbatim; three stop identities and what bumps each; enable bit stops cadence not liveness, `STANDING_LIVENESS_MS` = half silence, section-less; stop on failed exchange stops beat, never retries, raises revalidation not stale; stop inside durable Apply = relation failure not rollback; core1 reads no live QMK state; HP core1 roles; discovery Left-initiated. Delete 326-331 why-not-a-queue, 372-374/400-406/431-434 stories | 48 |
| #### One carrier for the response section set | 455-508 | 54 | K 85% / D 15% | rule (rides standing answer only; core0-lane request gets bare ACK; eligibility untouched); one-line cause (initiator core1 edge cache; second path makes it hold a stale value); generalization line; responder direction out of scope; suppression on sender side because shadow retires from the wire's section byte; three REFUSED blocks 491-507 verbatim. Delete "paid for twice" | 26 |
| ## Replacement Storage Route | 509-592 | 84 | K 75% / T 15% / D 10% | pointer line; cold-task-only selection + scan hooks consume cached token only; initiator admission condition list (6 terms, 519-523) as table; responder never selects; BUSY/pin handoff nonexclusive, TRANSFER clamps owner routes + enable bit as a mask down to `ATTACH_STATUS`, lifts at transfer-verified, cancel triggers; arbitration exchanges nonexclusive at retry cadence; three initiation triggers; news carrier = HSRSP storage news section in both relations; hint consumed as news never level; supersession rules; post-push rotation reopens via `SESSION_STATUS`; close/abort flushes matrix relation, MATCH doesn't; one lane not one per relation; mask admits only `SESSION_STATUS` while exclusive. 540-547 -> pointer to Common Route Priority | 28 |
| ## SESSION_STATUS Discovery And Liveness | 593-710 | 118 | K 35% / P 35% / D 30% | discovery/revalidation only, no advisory bit (0x04 retired); bullets 602-616 as rows (no policy gate; bootstrap 25 ms -> 500 ms after 10 misses; liveness regardless of class -> `era_invariants.md`); boot Low rule (2 lines); listener ring -> pointer to `split/era_split_link.h` **Reconciliation** + one line "cable powers the peer, so every plug is a late-peer case: threshold two"; the three route-layer terms + step in scheduler teardown window + dwell assert (3 lines, this is what the index routes here for); raise: relation-open evidence, T_commit via time anchor, `apply_link_level()` window, failed raise reverts to Low and stores nothing, first EEPROM SYNC waits for `link_runtime_settled()`, winner = DH Left / HP HOST, Left = the arbitrary-tie answer. Delete 626-645 mechanism retelling and 663-672 silence-floor story (refusal lives at `era_split_link.h:221`) | 26 |
| ## HOST-PEER PEER Priority | 711-737 | 27 | K 80% / D 20% | 3-item order; standing exchange not a 4th entry (position in core1 pass; source-push preempts it there); MUST suppress duplicate refresh, MUST NOT suppress discovery/liveness/revalidation. 721-724 -> pointer | 9 |
| ## HOST-PEER HOST | 738-760 | 23 | T 60% / K 40% | 6 bullets as rows; HSRSP response-slot-only -> pointer to `era_closed_surface_contract.md`; "busy HOST answers the slot with section-less ACK" | 8 |
| ## DUAL-HOST | 761-801 | 41 | K 50% / T 40% / D 10% | pointer to `era_authority_contract.md`; matrix/digest/mirror closed; storage Left->Right; runtime open, Right grants nothing; section list row; three consequences (poll unconditional, ACTIVITY window derivation, Right = time authority); owner record 2026-08-09 line; producer-path rows (Left route-due bit: INPUT edge, activity change, visual baseline; Right: snapshot rebuild). 798-800 merge into grant's eligibility rule | 12 |
| ## Stale Recovery | 802-829 | 28 | K 90% | watch counts every accepted frame (pointer to invariants); `RESPONDER_SILENCE_MS` 100 canonical here, both polls asserted to half, `SESSION_STALE_MS` 0 selects it (`relation_stale_ms()`, file); "every 100 ms elsewhere is an application"; HOST close invalidates both-hosted facts -> peer-unknown bootstrap; failure sites mark dirty/due only; cable removal -> relation unknown | 10 |
| ## Due/Deadline Model | 830-959 | 130 | K 45% / P 20% / D 35% | event+deadline model; dead-core1 detector table (sensor / word / site / file); initiator-watch arm gate list (5 terms; granted-plan gate is device-established: ungated it fires on a powered-without-USB pair); in-flight expiry; bound = `CORE1_UNRESPONSIVE_MS` = `SILENCE_MS`, never the override, assert at equality; two-stage convergence, judgment owner-layer; RX wait rules; PEER matrix dirty marks route due without planning; no runtime deadline in core0 set; pre-filter rule (stamp early-never-late, `timer_expired32()` still owed, file); two REFUSED blocks 923-931, 933-941 verbatim; one-clock fact (`CH_CFG_ST_FREQUENCY` 1 MHz = `TIMERAWL`) + "three alternatives each buy 0.021 us" line. Delete 914-921 re-take instruction (-> `era_capture_reading.md`), 943-958 essay | 46 |
| ### Latch Concurrency Rules | 960-983 | 24 | K 80% / D 20% | producer contexts line; OR-only under `ATOMIC_BLOCK_RESTORESTATE`, never clear/never plan; consumer atomic read-and-clear; edge-not-level rule + generalizable form; no latch from raw SOF before reducer / setter before generation commit; init seeds snapshot. Delete storage-hint story (976-977) | 9 |

**Totals: 983 -> ~310 lines (~31 %).**

### 2. MUST-SURVIVE checklist

REFUSED blocks (verbatim, all three lines):
- 383 `publish the response plan inside the responder snapshot instead of threading it.`
- 491 `delete core1's receive-side edge filter.`
- 497 `drop the section decode at the receiver instead of suppressing at the sender.`
- 503 `keep the second arrival path and fold its value back into core1's receive-side cache on every arrival, rather than deleting the path.`
- 923 `let the raw pre-filter decide outright and drop the millisecond compare.`
- 933 `inline the gate's event-arm accessors into a header, since each compiles to 12-24 bytes behind a full \`bl\`.`

Values (all verified against the tree):
- `ERA_SPLIT_SESSION_REFRESH_PERIOD_MS` 50 ms - known peer, lane not live (147)
- `ERA_SPLIT_HOST_SOURCE_RESPONSE_POLL_PERIOD_MS` 10 ms; `ERA_HOST_PEER_RGB_STATE_SNAPSHOT_PUBLISH_PERIOD_MS` == it, arms on DH Right too (170-183)
- `ERA_SPLIT_PEER_RESPONSE_WINDOW_MS` 20 ms, not a cadence (184)
- `ERA_SPLIT_DUAL_RUNTIME_POLL_MS` 1 ms; 1/2/4 ms High/Medium/Low = x`wire_scale()` (202, 296, 300-303)
- queue-residence window = 20 ms x scale + 5 ms (`REQUEST_HANDOFF_MARGIN_US` 5000) = 25/45/85 ms; Low max < 100 ms asserted (37-48)
- worst-case DH exchange ~0.80 ms at 1 ms/460800, 0.2 ms idle (229-230, 310-311)
- storage: audit sweep + retry `ERA_HOST_PEER_STORAGE_RETRY_MS` 25 ms; `DIRTY_QUIET_MS` 1000 ms; `EPISODE_MS` 5 s; streak <= `PEER_SILENCE_MS/RETRY_MS`; backoff `RETRY_MS << streak`, `BACKOFF_MAX_SHIFT` 6, `BACKOFF_MAX_MS` 1000; live iff `ERA_SPLIT_EEPROM_SYNC_ENABLE` (243-260)
- bootstrap `ERA_SPLIT_WIRE_BOOTSTRAP_PERIOD_MS` 25 ms -> `..._BACKOFF_PERIOD_MS` 500 ms after `..._BACKOFF_AFTER` 10 misses (608-612)
- `ERA_SPLIT_RESPONDER_SILENCE_MS` 100 (canonical here); `ERA_SPLIT_SESSION_STALE_MS` 0 = use constant; both polls <= half (809-819)
- `ERA_SPLIT_STANDING_LIVENESS_MS` = silence/2, not per-relation (391-398)
- `ERA_SPLIT_CORE1_UNRESPONSIVE_MS` == `RESPONDER_SILENCE_MS`, never the STALE override; 2 x liveness <= it at equality (873-880)
- `ERA_SPLIT_LINK_SCAN_DWELL_MS` dwell; listener threshold two undecodable arrivals; ring High->Medium->Low->High (633-640)
- `CH_CFG_ST_FREQUENCY` 1,000,000; `timer_read32()` ~0.42 us; exact stamp worth 0.021 us (919-920, 931, 944, 957)

Owner decisions (one line each):
- 2026-08-01: DH runtime cadence is one period, run unconditionally - no activity window/quiet rate/hint (201-206)
- 2026-08-09: HP poll 20->10 ms, taken on worst-case arithmetic, settled by capture reading not build (172-179)
- 2026-08-09: DH poll 2->1 ms (202-203)
- 2026-08-09 owner record: RGB SYNC targets complete synchronization, behaving as HOST-PEER (791-792) - appears in no other document

Device-established facts (one line + what they ground):
- 2026-08-16: initiator core1 busy-polled the wire, 258 us/exchange -> WFE-parked 160 us, one park per byte; grounds "period's bound is the wire" being true of the parked implementation; the sleep fractions live in `era_performance_gates.md` **Fixed Baselines** (216-224)
- the ungated initiator silence watch fired every bound on a healthy powered-without-USB pair -> granted-plan gate is load-bearing (862-864)
- DH Left without a publish-side consumer of the route-due bit failed `scan_idle()` per scan until housekeeping (372-374)
- the retired activity-gated two-rate cadence: level-armed latch made the period unreachable -> device check = poll rate far above configured period (237-242)
- silence-drop-to-High recorded a working level as failed on both halves (663-672) - keep as the one-line WHY beside the pointer, or drop since `era_split_link.h:221` holds the refusal
- the cable carries power from the hosted half, so every plug is a late-peer case -> threshold two (640-643)

Headline rules: priority applies at transaction boundaries, never preempts (23); nothing core0 originates runs on a cadence in either relation (157); `SESSION_STATUS` has no post-relation cadence (134); one route kind on core1's period in every serviced relation, the two runtime reasons and nothing else (288-293); the period is the only per-relation term (295); plan published on change never on pass/timer (335); grant stops on epoch/generation/enable change (387); core1 never retries, failure is core0's (426); a stop raises revalidation, does not declare stale (436); response sections ride the standing answer only (457); initiation is change-triggered never periodic (552); hint consumed as news never level (561); exclusivity suppresses owner routes never a responder's answer (75, 540); every half opens the wire at Low (617); no runtime route deadline in core0's deadline set (899); raw pre-filter never the authority (904); latches OR-only, consumed as edges (966-979).

### 3. ROUTED HEADINGS (must keep exact text)

- **One carrier for the response section set** (line 455) - tool-checked from `era_host_peer_matrix_contract.md:76`, `era_invariants.md:387`, `era_wire_contract.md:877`; also named (unchecked forms) by `era_authority_contract.md:378-379` (wrapped), `era_active_index.md:88`, `era_feature_path.md:37`.
- **Due/Deadline Model** (830) - `era_walkthrough.md:51` in the `(doc, Section)` form; note the checker's `SECTION_PAREN` class excludes `/`, so this one is not machine-checked - keep it anyway.
- **The standing exchange grant** (286) - `era_active_index.md:88` and `era_feature_path.md:37` ("the standing exchange grant"), `era_host_peer_matrix_contract.md:53` ("grant section"), source comment `split/era_split_wire_router.c:63`.
- **Common Route Priority** (11) - source comment `split/era_split_transport_scheduler.c:143`; the document's own line 540 routes to it.
- Because `headings()` in `era_doc_refs.py:241-256` also indexes every `**bold lead-in**`, no other file routes to a bold lead-in of this document (grep confirmed). Outgoing routes this file makes that must stay resolvable: `era_host_peer_storage_contract.md` **Arbitration** (:296), `era_performance_gates.md` **Fixed Baselines** (:428), `split/era_split_link.h` **Reconciliation** (:115, not a `.md`, unchecked).

### 4. CHECKER TRAPS

Function-claim paragraphs (all currently clean - each already holds a backticked `.c` token; when compressing, the file token must stay in the **same blank-line-delimited block** as the `foo()`): 300-314 (`era_split_transaction_backend_wire_scale()`), 353-362 (`scan_idle()`), 364-374 (`publish_standing_plan()`, `refresh_route_due_flags()`, `scan_idle()`), 617-624 (`start_communication_core()`), 680-692 (`apply_link_level()`, `wire_scale()`), 694-698 (`era_split_link_runtime_settled()`), 809-819 (`relation_stale_ms()`), 843-871 (`era_split_communication_core_progress_count()`), 904-921 (`timer_expired32()`, `timer_read32()`), 943-958 (`st_lld_get_counter()`, `timer_read32()`). A `.md` name does not count as a file.

Dated lines: 172 keep (owner decision) / 202 keep (owner decision) / 203 keep (owner decision) / 216 keep, one line (rule-evidence) / 791 keep (owner decision). No report-class dates to delete.

Stale cross-pointer: line 251-252 says the failure-streak derivation "is `era_host_peer_storage_contract.md`'s" - that contract contains no such derivation (grep for silence/streak: nothing). It lives in `split/era_host_peer_storage.h:148-149` (`PEER_SILENCE_MS = 2 x PEER_STALL_WORST_MS`, 500 ms). Point there instead.

### 5. Duplication - canonical owner elsewhere

- 157-164, 400-406, 613-616, 804-808: liveness regardless of class, core0 originates no periodic frame, liveness must not depend on the half that can stop -> `era_invariants.md:96-114` (which itself defers the period and stop semantics back here - keep only those here).
- 446-449 and 278-284: CORE1_FULL / core0 owns route-snapshot-apply -> `era_invariants.md:121-133`.
- 626-645, 663-672: listener ring mechanism and the silence-floor refusal -> `split/era_split_link.h` **Reconciliation** (:115-249, REFUSED at :221).
- 60-73: CLEAN quarantine semantics -> `era_host_peer_storage_contract.md` **Why An EEPROM Clean Is An Agreed Restart** (:487); keep only the route-admission row here.
- 248-249: 1000 ms quiet interval also stated at `era_host_peer_storage_contract.md:165` - one of the two should become a pointer (this file claims "all service cadences").
- 704-709: Left tie -> `era_host_peer_storage_contract.md:308` and `split/era_split_restart_agreement.h:48`.
- 746-751: HSRSP response-slot-only -> `era_closed_surface_contract.md:64`; 744-745 one-byte ACK names -> `era_invariants.md:96-97`.
- 767-769: DH matrix/digest/mirror closure -> `era_invariants.md` (the "closure below" at :66-73).
- 316-324: `matrix_ready` definition -> `era_authority_contract.md:331-352`; keep only the arm precondition here (the matrix contract routes here for it).
- 216-224: core1 sleep fractions -> `era_performance_gates.md` **Fixed Baselines**; 919-921, 943-950: timer millisecond cache -> `era_qmk_fork_ledger.md`; 914-917: how to re-take the `wire maint entry=` rate -> `era_capture_reading.md:978` (manual genre, not contract).
- 972-979: storage hint history -> `era_host_peer_storage_contract.md` **Storage News Value And Relation-Open Audit** (:317).

---
## A4. `contracts/era_invariants.md` (395 lines) and `contracts/era_authority_contract.md` (542 lines)

**Cross-cutting finding:** neither document contains a single `> **REFUSED:**` block (grep for `REFUSED|REOPENS` returns nothing), yet both hold ~14 refusal-shaped sentences ("X is the rejected alternative", "MUST NOT be reintroduced", "does not substitute: 2000U was tried"). The restructure should convert those to the three-line form; candidates are listed per document. Today both docs pass the checker's function-claim rule only because whole bullet runs are one blank-line paragraph (`re.split(r"\n\s*\n")`) sharing one `.c` mention - splitting into rows exposes the traps listed in 4.

### A4-a. `era_invariants.md` - 395 -> ~120 lines

Only two `##` headings exist; Hard Invariants is classified by bullet cluster.

| Section / cluster | Lines | N | Verdict | Survives | After |
|---|---|---|---|---|---|
| Header | 1-8 | 8 | K | two lines after Phase 3 | 6 |
| Hard Inv.: QMK fork hygiene | 12-36 | 25 | T 60 / K 15 / D 25 | 3 rows: `matrix_common.c` pristine; `matrix.c` two departures (`quantum/matrix.c:24..386` hooks, `:394-395` split); third hook = selector+consumer same change. One clause: not compiled into any ERA image (`era_split_qmk_rules.mk:201-202`), verify by diff. L16-20 story, L21-25 reasoning -> D | 6 |
| HID producer / transport RX | 37-42 | 6 | T | 3 rows | 3 |
| Discovery + HOST-PEER traffic shape | 43-53 | 11 | T 70 / P 30 | 4 rows; L47-52 storage-lane enumeration -> pointer to storage contract Relation Admission | 4 |
| DUAL-HOST ownership + traffic set | 54-64 | 11 | T | 3 rows | 3 |
| "key input stays off the wire" essay | 66-92 | 27 | K 30 / D 70 | 3 rows: no key input on DH wire (SA `split/era_split_wire_protocol.h:599`); visual baseline = render state, MATRIX section closed; runtime sections latest-state + edge-armed. Anchor-every-60 s clause -> capture-reading pointer | 4 |
| matrix closure / `matrix_ready` / control ids | 93-97 | 5 | T | 3 rows | 3 |
| Liveness | 98-109 | 12 | T 50 / K 30 / D 20 | 2 rows; "one busy responder collapsed a pair for tens of seconds" = one grounding clause | 3 |
| One architecture + `SESSION_STATUS` role | 110-120 | 11 | T 70 / D 30 | 2 rows (second duplicates authority Revalidation) | 2 |
| `CORE1_FULL` / core1 RX-TX / timing grant | 121-138 | 18 | T 50 / P 50 | 3 rows; grant identity terms -> route contract | 4 |
| Replacement Apply boundary | 139-148 | 10 | K 70 / P 30 | 1 row + pointer; must name `storage/era_nvm.c` (trap) | 2 |
| Build-variant identity | 149-155 | 7 | P | 1 row -> `manuals/era_build_options.md` | 1 |
| Control-byte ownership | 156-158 | 3 | T | 1 row | 1 |
| No ChibiOS APIs on core1 actor | 159-161 | 3 | T | 1 row | 1 |
| SPSC rings + three exclusions | 162-167 | 6 | K / T | 1 row + 3-row table (excluded thing / why) | 4 |
| SIO FIFO IRQ mask | 168-171 | 4 | T | 1 row | 1 |
| Core1 PSM halt | 172-197 | 26 | K 35 / D 65 | 1 row (window `crt0_v6m.S:249-286`, entry `:196`, `system/era_boot_core1_halt.c`); why = one line (SYSRESETREQ resets only its core, datasheet 2.4.2.9; SDK precondition); REFUSED: cause-side guard. L175-179 failure narrative -> D | 7 |
| Pre-copy placement / magic / scratch0 / bounded waits | 199-232 | 34 | T 50 / D 30 / P 20 | 4 rows (no SRAM object but `magic_location`, second reopens; scratch0 = reset-domain MMIO; every wait bounded incl. PSM ack; clear `FRCE_OFF` before return) + device fact one line (scratch0 clear on first post-UF2 boots -> joint-tuple discriminator). L207-212 justification -> D; carve-out rules -> ld pointer | 7 |
| No flash VMA after crt0 / vectors | 234-246 | 13 | K 40 / D 30 / P 30 | 2 rows; "believed true while false in dozens of slots" -> D; PRIMASK trade -> sram residency | 3 |
| Zero reset-cause branches / timer rebase / classifier / ROSC policy / closer | 248-296 | 49 | K 40 / T 25 / D 35 | 8 rows: no branch on cause + unconditional attach; marker needs contract+device evidence; pre-`keyboard_init()` stamp not comparable; classifier = one bounded exception; ROSC 768 x 240 = 184,320 / 256 polls / no clock writes; 15.36-102.4 ms, 20 ms objective, 1000 ms timeout, <=2000 ms observed; closer started-bit != timestamp, zero valid, disarm clears three; disassembly proof -> perf-gates pointer. REFUSED: deferred-attach gate | 14 |
| Software exits clear magic / split across copy / three states / both disarm | 298-327 | 30 | K 45 / D 55 | 4 rows + REFUSED (raising `RP2040_BOOTLOADER_DOUBLE_TAP_RESET_TIMEOUT`); hit-rate ~1/10 and bounce story -> one clause each | 9 |
| Single named launch step | 329-339 | 11 | K | 2 rows (step returns `bool`; `_init()` policy-only; `ensure_core1()` unreachable before) | 3 |
| `request_quiesce()` != halt | 341-346 | 6 | K | 1 row | 2 |
| NVM flash no recursion | 347-354 | 8 | K | 2 rows (trap: needs a `.c`) | 2 |
| Macro transaction scope | 356-363 | 8 | K 60 / P 40 | 1 row + storage-contract pointer | 2 |
| Responder-result capacity | 364-375 | 12 | K 40 / D 60 | 2 rows; L369-375 story -> D (and "three slots" is stale: tree has `..._RESPONDER_GENERAL_RESULT_SLOTS 4U`, `..._SOURCE_RESULT_SLOTS 2U`) | 3 |
| Stop Conditions | 377-395 | 19 | T | 7-row table (tripwire / contract that could open it) | 10 |

**1. Totals:** 395 -> ~120 (+~10 headings/blank lines).

**2. MUST-SURVIVE**
- REFUSED blocks: none exist. Convert: cause-side reset guard as primary (L192-197); deferred USB-attach gate / reset-cause marker (L252-257); raising the double-tap timeout, 2000U tried (L310-312); `queue.c`/hw spinlocks/inter-core FIFO (L162-167); a second retained pre-copy object (L204-205); quiesce as halt (L341-346).
- Constants (all verified against tree): `MATRIX_SCAN_{RAW,COUNT}_DIAGNOSTICS_ENABLE` (L16); `ERA_RP2040_MATRIX_ENABLE = yes` -> `CUSTOM_MATRIX` (L30-32, `era_split_qmk_rules.mk:201`); `CORE1_FULL` (L121); `crt0_v6m.S:167/196/249-286` (L174/188/226 - 167 `cpsid i`, 196 `bl __early_init`, 285 `bl __init_ram_areas`: correct); datasheet 2.4.2.9/2.4.8 (L181); `magic_location` (L203); 184,320 = 768 x 240 ticks, 256-poll bound (L274-275; `system/era_boot_core1_halt.c:100-102`, SA `:111-112`); 1.8-12 MHz -> 15.36-102.4 ms, 20 ms (L280-282); 1000 ms `RP2040_BOOTLOADER_DOUBLE_TAP_RESET_TIMEOUT` (L284, board `config.h` = 1000U); <=2000 ms (L286); 2^32 (L262); 60 s anchor (L90, `ERA_SPLIT_TIME_ANCHOR_REFRESH_MS 60000`); `anc`/`sec=0x80` (L91); 4-KiB sector (L351); PSM ack bound is `ERA_BOOT_CORE1_HALT_ACK_SPINS 1000` (unstated; add to the row). Stale: "three slots / 150 ms" (L373).
- Owner decisions: none dated here; the two-departure cap (L13-27) and `CORE1_FULL`-only (L121) are decision-shaped.
- Device facts to keep as one line each: busy responder collapsed a pair for tens of seconds (L107-108); scratch0 clear on both first post-UF2 boots, marker persisted across core-only reset (L218-222); late arm -> ~1/10 tweezer hit rate, 2000U moved it not at all (L306-312); bootloader bounce until replug with only `mcu_reset()` disarming (L322-325); vectors false in dozens of slots (L240-241, grounds the gate).
- Invariants (bullet start line -> enforcement; SA=`_Static_assert`, LD=linker ASSERT/`.vectors` gate, SG=Source Gate diff/nm, MEM=agent memory only):
  L12 `matrix_common.c` byte-identical - SG. | L13 `matrix.c` exactly two departures, standard == upstream; third hook needs selector+consumer - SG; MEM. | L37 transport RX injects no HID - MEM. | L38 RX updates caches only; QMK matrix is HID producer - MEM. | L40 HID authority local; no remote endpoints; projection via local matrix path only - MEM. | L43 peer-unknown = Left-only `SESSION_STATUS` - mode planner; MEM. | L44 peer-unknown Right never sends - MEM. | L45 HOST-PEER traffic PEER-initiated; HOST answers only in admitted slot; storage lane same shape - MEM. | L53 HOST independent send closed - MEM. | L54 DH Left initiator / Right responder - mode planner; MEM. | L56 DH traffic = `SESSION_STATUS` + storage lane + runtime exchange; storage change-driven; Right never sends - MEM; matrix closure SA `era_split_wire_protocol.h:599`. | L66 key input off DH wire; visual baseline = render state; MATRIX closed - SA `:599`. | L83 runtime sections latest-state, edge-armed - MEM. | L93 DH never opens matrix/digest/mirror - SA `:599`. | L94 `matrix_ready` hard gate - MEM (authority Matrix Admission). | L96 heartbeat/ACK_STATUS one-byte, no class/op ids - MEM. | L98 every accepted frame refreshes liveness; no exemptions - MEM (`..._responder_service.c:614`). | L104 liveness never on the half that can stop; core0 no periodic frame - MEM. | L110 one runtime architecture per relation - MEM. | L115 `SESSION_STATUS` bootstrap/recovery only, no cadence - MEM. | L121 `CORE1_FULL` only stage - build resolver; MEM. | L122 all RX/TX on core1; no core0 lease/executor/adapter/switch/fallback - SG retired-path; MEM. | L127 timing grant bounded, per relation/route kind, never authority; stop on mismatch, never resume after failure - MEM. | L139 one durable boundary = NVM commit; old bytes until commit; no rewrite-back - MEM (`storage/era_nvm.c`). | L149 variant name == five-axis tuple - launcher ELF witnesses; make. | L156 core1 owns control byte + tx/ack seq - MEM. | L159 no ChibiOS suspend/resume/sched-lock on core1; IRQ = hint - MEM. | L162 static SPSC rings; three exclusions - SG no-allocator (`era_residency_gate.sh:78`); MEM. | L168 mask core0 SIO FIFO IRQ over handshake - MEM. | L172 PSM `FRCE_OFF PROC1` at destroy-window entry from pre-copy hook; cause-side guard never primary - LD pins TU + ASSERT; MEM. | L199 pre-copy touches no SRAM but `magic_location`; scratch0 = MMIO; all waits bounded; clear `FRCE_OFF` - LD (`era_performance_gates.md:419`), SA `:111-112`, disassembly leg; MEM. | L234 no flash VMA after crt0; `.vectors` SRAM except reset slot - `.vectors` gate + LD. | L248 zero reset-cause branches; unconditional attach; marker needs entry+evidence; pre-init stamps incomparable; classifier sole exception; ROSC fixed policy; closer started-bit - SA `:112`; disassembly gate; MEM. | L298 software exits clear magic; arm pre-copy / jump post-copy never move; three states; `__late_init()` never re-arms; both `mcu_reset()` and `bootloader_jump()` disarm - MEM (+LD carve-out for veneer). | L329 single named launch step returns `bool`; init policy-only - MEM. | L341 quiesce != halt - MEM. | L347 NVM no recursion; <=1 sector per `era_common_features_task()` - MEM. | L356 macro txn blocks only macro-domain durable ops - MEM. | L364 reserve result capacity before result-bearing success; bare-poll ACK exempt - static slot constants; MEM. | Stop Conditions x7 (L382-395) - MEM.

**3. ROUTED HEADINGS:** none. Every external reference is file-level (27 doc/rule lines, 18 source-comment lines). Not machine-checked but reader-facing: `era_wire_contract.md:333-334` and `era_split_wire_protocol.h:166` cite "`era_invariants.md`'s render-state clause"; `era_active_index.md:105` "boot-safety cluster", `:112` "bounds `quantum/matrix.c`"; `era_route_contract.md:284` "fallback forms that are forbidden"; source comments cite: watch-has-no-exemptions (`responder_service.c:614`), launch step (`era_split_board.h:38`), wake-is-a-hint (`backend_rp2040.c:810`), no-allocator (`era_common_qmk_rules.mk:37`, `rgblight.c:1437`), nothing-branches-on-cause (`era_rp2040_matrix_pio.c:47`), second-object-reopens (`era_boot_core1_halt.c:72`), DH-matrix-closed (`wire_protocol.h:599`). Both `##` headings are free to rename; "Stop Conditions" is named in the file's own header (L7).

**4. CHECKER TRAPS** (checker: paragraph = blank-line block; needs `` `name()` ``, a verb from returns/calls/sets/clears/holds/reads/writes/advances/gates/excludes/raises/counts/runs/fires/owns, and no `` `*.c/.h/.mk/.py/.ld/.json` `` or `path.c:N`; note `crt0_v6m.S:N` is CITE-checked but is NOT a file ref for this rule). Currently zero failures. After splitting: L139-148 `era_nvm_replace()` "owns" -> cite `storage/era_nvm.c:1015`; L347-354 `keyboard_task()`/`matrix_task()`/`era_common_features_task()` "call"/"returns" -> cite a `.c` (today saved by sharing L341's lifecycle file); L172-185 safe (`NVIC_SystemReset` uppercase, `__init_ram_areas` outside roots - today saved by `` `queue.c` `` at L163). Dates: none.

**5. Duplication:** L45-52, L56-64 storage-lane enumeration -> storage contract Relation Admission; L66-92 period -> route contract, margin -> perf gates, section discipline -> wire contract, `anc` -> capture reading; L115-120 == authority L471-478 (same rule in both audited docs); L127-138 -> route contract (owns terms); L139-148 -> storage contract (order); L149-155 -> `era_build_options.md`; L207-212, L240-246 -> sram residency; L224-232 carve-out rules -> `ERA_RP2040_SRAM_RESIDENT.ld`; L272-286 ROSC policy also restated at `era_source_map.md:170`; L329-339 restated at `era_source_map.md:24`; L356-363 -> storage contract transcript.

### A4-b. `era_authority_contract.md` - 542 -> ~170 lines

| Section | Lines | N | Verdict | Survives | After |
|---|---|---|---|---|---|
| Header | 1-8 | 8 | K | two lines after Phase 3 | 6 |
| Local Authority Facts | 10-73 | 64 | T 70 / K 20 / D 10 | facts table (`accepted_host_open`, `accepted_no_host` exactly-one, `bulk_page_supported` compile-time); closes table (3 rows -> `accepted_no_host`: sustained suspend past `ERA_SPLIT_AUTHORITY_SUSPEND_HOST_GRACE_MS` 500; suspend with no configuration; not CONFIGURED or SOF stale > `ERA_SPLIT_AUTHORITY_SOF_FRESH_MS` 10); rule rows: single derivation = reducer, no boot latch/probe; `is_keyboard_master()`/`_impl()` projections; override MUST stay (SG: no `usb_disconnect` symbol); handedness from `SPLIT_HAND_PIN`; no raw VBUS, `SPLIT_USB_DETECT` required / `USB_VBUS_PIN` forbidden; firmware reattach not a close; sleep not gated on host-open. Each "why" -> one clause; L64-73 -> pointer | 22 |
| Lighting Sleep Ownership | 75-119 | 45 | K 50 / D 50 | table L80-83 verbatim; 2 edge rows (local->wire resolves lit; wire->local drops wire's word); override rule (status report punches gate 3.68 s; HOST publishes resolved decision, never raw gate); L103-112 premise story -> D, 2026-08-13 fact one line | 14 |
| remote-wake bullet (misfiled here; belongs with Local Facts) | 120-146 | 27 | K 40 / D 60 | 4 rows: exactly two local inputs; never gated on host-open / `is_keyboard_master()` / action pipeline; waking input consumed locally; PEER key only as HOST's composed edge -> Relation Hold. Regression + 2026-08-13 fact -> one line | 6 |
| Persisted Sync Policy | 148-266 | 119 | T 45 / K 25 / D 30 | bits table (family / scope / sender-receiver arms / default / since-version) 3 rows; block-layout table 8 rows; 3 rules (v5 only, invalid block rewritten whole incl. counters, changed default rides version byte: v4 INPUT, v5 RGB); persist-opens-nothing row; link level -> REFUSED + `split/era_split_link.h` pointer; L217-225 clean -> storage-contract pointer; L176-180, L183-188, L255-260 justifications -> D | 27 |
| Replacement Storage Authority | 268-327 | 60 | K 55 / P 25 / D 20 | 8 rule rows (initiator-only initiation; VIA toggle two arms incl. `POLICY_CLOSED`; one identity space; completion owner = applying half's core0; supersession = data event; `bulk_page_supported` compile-time == `ERA_HOST_PEER_STORAGE_V1_ENABLE`); match-set row (8 terms + chunk identity); REFUSED: fixed HOST-source/PEER-target winner; L270-274, L294-297 -> pointers | 14 |
| Matrix Ready | 329-341 | 13 | T | one truth table | 6 |
| HOST-PEER Matrix Admission | 343-362 | 20 | T 50 / D 50 | 6-condition row; "role term is the relation, not `accepted_host_open`" one line; rest -> D | 7 |
| HOST-PEER HOST Source Response Admission | 364-381 | 18 | T 60 / P 40 | 3-condition row + bare `HOST_PEER_ACK_STATUS` rule; carrier -> route-contract pointer | 5 |
| Relation Hold | 383-444 | 62 | T 35 / K 30 / D 35 | transition table (authority pair x liveness -> relation; 5 rows incl. hold and stale-reset); fold-over-edges rule; held exchange keeps 10 ms HP / 1 ms DH indefinitely; >=100 ms outage or core1 death -> `LOCAL_NO_LINK` hold, PEER-key wake lost, HOST-key wake survives; three-sites-one-rule line. L415-421, L432-438 -> D | 14 |
| Initiator Authority | 446-467 | 22 | K 60 / T 15 / D 25 | table L455-460 verbatim + runtime-route row; "projection of the relation, never re-derived" one line | 10 |
| Revalidation Authority | 469-522 | 54 | T 60 / K 25 / D 15 | 9 rows; poll-period row: `ERA_SPLIT_AUTHORITY_POLL_PERIOD_MS` == `..._SOF_FRESH_MS` (10), SA `scheduler/era_split_transport_scheduler_internal.h:171`, 10-20 ms detect; forced stale recovery + REFUSED (forcing against an unambiguous no-host peer) | 16 |
| Authority Scheduler Boundary | 524-542 | 19 | T 80 / D 20 | 7 rows; "one poll, never event-driven" keeps its one-sentence why | 9 |

**1. Totals:** 542 -> ~170 (+~12 headings/blank lines).

**2. MUST-SURVIVE**
- REFUSED blocks: none exist. Convert: fixed HOST-source/PEER-target winner (L280-287); syncing the link level through the policy block (L205-215); in-place upgrade (L250-251, owner decision recorded in source map); forced stale recovery against an unambiguous no-host peer (L514-522); event-driving the SOF poll (L527-533); wake requester behind the action pipeline (L123-128); removing the `is_keyboard_master_impl()` override / raw VBUS (L25-41); second copy of role term via `accepted_host_open` (L354-362, L440-444).
- Constants (verified): `ERA_SPLIT_AUTHORITY_SOF_FRESH_MS` = 10 (L52, L495; `split/era_split_authority_reducer.h:19`); `ERA_SPLIT_AUTHORITY_POLL_PERIOD_MS` == SOF_FRESH (L496; `..._internal.h:153`, SA `:171`); 10-20 ms, 2048 ms wrap (L499-500); block offset 176 size 24 via `ERA_EEPROM_CONFIG_ADDR + ERA_EEPROM_LOCAL_POLICY_CONFIG_OFFSET` (L229-230; `storage/era_eeprom_layout.h:93-94`, SA `:176-177`); offsets +0/+4/+6/+8/+9/+10..+23 (L233-240; SA `split/era_split_sync_policy.c:54-60`; `..._COUNTER_OFFSET 10`, `..._PREFIX_BYTES 10`); version 5 only (L238, L250; `split/era_split_sync_storage.h:29`); all three flags default on (L239; SA `era_split_sync_policy.c:34`); `ERA_HOST_PEER_STORAGE_V1_ENABLE` (L324); 3.68 s (L115; 3680 ms `..._launch_signal.c:18`); 10 ms / 1 ms (L410; `ERA_SPLIT_HOST_SOURCE_RESPONSE_POLL_PERIOD_MS 10`, `ERA_SPLIT_DUAL_RUNTIME_POLL_MS 1`); >=100 ms (L423; `ERA_SPLIT_RESPONDER_SILENCE_MS 100`); >=18 kHz (L419, perf gates owns); seven-byte AUTHORITY section (L491, wire contract owns); `SPLIT_HAND_PIN`, `SPLIT_USB_DETECT`, `USB_VBUS_PIN` (L31, L37); `POLICY_CLOSED` (L301); `HOST_PEER_ACK_STATUS` (L375); `local_status_pending` (L504). Unstated but row-worthy: grace = 500 ms (`era_split_authority_reducer.c:22`).
- Owner decisions: RGB bit default ON since v5, 2026-08-13 (L175); INPUT bit default ON from v4 (L181-184); no in-place upgrade, v5 only (L250); latest-change-wins arbitration over fixed winner (L276-287); `host_peer` naming retained (L272-274).
- Device-established facts (one line each): 2026-08-13 demoted DH->PEER half dark until other-half key -> grounds "resolve lit" (L110-112); 2026-08-13 HOST key wakes, PEER key does not -> grounds Relation Hold (L143-144); wake requester behind action pipeline shipped as regression -> grounds ungating (L126-128); one-half clean converges seven domains but leaves 176..255 split (L218-221; storage contract owns).

**3. ROUTED HEADINGS** (must keep exact text): **Lighting Sleep Ownership** <- `era_wire_contract.md:741`, `era_board_adoption.md:285` (machine-checked); **Matrix Ready** <- `era_wire_contract.md:165-166` (bold wraps across lines, so not machine-checked today - keep anyway); **Relation Hold** <- `era_walkthrough.md:240` parenthetical form (machine-checked). Source-comment routes (not machine-checked): **Initiator Authority** <- `split/era_split_mode_planner.c:89`, `.h:56`. Internal bold cross-references to preserve consistently: **Relation Hold** (L144, L357, L437, L520), **Local Authority Facts** (L437, L538), **Revalidation Authority** (L440), **HOST-PEER Matrix Admission** (L442), **Lighting Sleep Ownership** (L73). All other 30 references (active index, identifier map, source map, walkthrough, overview, closed-surface, route/wire contracts, `.claude/rules`) are file-level.

**4. CHECKER TRAPS:** currently zero (L12-73 is one paragraph carrying three `.c` refs). After splitting into rows: L18-24 `is_keyboard_master()`/`_impl()` ("read by nothing") -> cite `split/era_split_authority_reducer.c:101-105`; L25-30 `usb_disconnect()` ("calls") -> cite `quantum/split_common/split_util.c:189`; L36-41 `usb_vbus_state()` ("read") -> cite `quantum/split_common/split_util.c:80`; L31-35 `split_pre_init()` has no listed verb but add the file. L120-132, L354-362, L541-542 already name files. Dates: L110 keep (rule-evidence); L143 keep (rule-evidence); L175 keep (owner decision).

**5. Duplication:** L64-73 detector model -> `era_board_adoption.md` **Non-Split Board Baseline**; L161-173 RGB arms restated at `era_identifier_map.md:176-178` (that map routes back here - it is the copy); L181-201 INPUT arms restated at `era_identifier_map.md:180-192`; L205-215 -> `split/era_split_link.h` **Reconciliation** and `era_identifier_map.md:203`; L217-225 -> storage contract **Why An EEPROM Clean Is An Agreed Restart** / **Arbitration**; L240-245 -> storage contract Recency Layer; L250-260 vs `era_source_map.md:427-431` - **circular ownership**: source map says "the rule is canonical in `era_authority_contract.md`" while L251 says the source map "carries the owner decision" - pick one owner; L270-274 -> source map; L276-287, L294-297 -> storage contract (says so itself); L321-327 wire form -> `era_wire_contract.md:121,141`; L374-381 -> route contract **One carrier for the response section set** + wire eligibility; L408-413 cadences -> route contract (owns period values per active index); L471-478 == `era_invariants.md:115-120`; L485-492 section body -> wire contract; L493-501 derivation is `era_split_authority_reducer.h:16` + SA - the file's own business under AGENTS.md's layer rule, so a one-row pointer; L526-533 exception is named in `era_overview.md`.

---
## A5. `manuals/era_performance_gates.md` (584 lines) and `manuals/era_build_options.md` (484 lines)

`era_performance_gates.md` carries an uncommitted diff (HEAD 580 -> tree 584 lines; hunks at **98-101** and **108-112**, both inside *Focused host-test set*), so that section is being edited by the NVM agent - restructure it after that lands.

Source spot-checks done with `git show HEAD:`/`git grep` only: every selector default in the options table equals `keyboards/era/era_build_options.mk` (HEAD); the six tuples equal `common/build_variants/*.mk`; rule-2 knob defaults (64/4/10000/0/128) equal `era_rp2040_matrix_pio.c:86,94,102` and `era_split_qwin_diagnostics.c:49,53`; the 32 KiB floor equals `era_residency_gate.sh:73` (32768); core1 reservation `STACK_WORDS 512` = 2048 B. Eligibility bytes not re-derived.

### A5-a. `era_performance_gates.md` - 584 lines

| Heading | Lines | N | Verdict | What survives | After |
|---|---|---|---|---|---|
| title + header block + preamble | 1-17 | 17 | K 60 / D 40 | title, header lines; 11-17 meta-commentary deleted | 6 |
| The Instruments Are In The Tree And Off In Release | 18-51 | 34 | K 40 / P 30 / D 30 | 3-step procedure (`era-build k:m <variant>`; bind `WIRE_DIAG`/`WIRE_DIAG_2`/`WIRE_QWIN` temporarily - shipped keymaps bind none; decode with `era_capture_reading.md`); rule: all selectors off = byte-identical release UF2; rule: never record a source-inferred figure as measured | 10 |
| What A Change Owes | 52-73 | 22 | K (already a table) | table 57-69 whole; 54-55 rule; 71-72 pointer. Fix `**Layout checks**` (62) -> `Layout Checks` | 17 |
| Focused host-test set | 74-122 | 49 | K 25 / T 45 / D 30 | command block 78-91 verbatim; 93-116 -> table test x what it pins (one row each of 12); 117-121 -> one line "make-only test != launcher; only the launcher derives `compiled_tuple` from the ELF" | 28 |
| Scheduler admission/liveness device gate | 123-138 | 16 | T 80 / K 20 | gate row: entry (both DH directions, each half as PEER, High/Medium/Low, `cause` image) x pass (`QUEUE_EXPIRED`=0, `fail`=0, `claim==tx==rx==publish`, dead/cap/reclaim/service-failure static, transfer/complete/apply +1 per op, abort/timeout/integrity/version/domain/queue-pub=0); one line why the scale matrix is mandatory + the 3 files | 8 |
| ERA NVM persistence device gate | 139-172 | 34 | T 70 / K 20 / D 10 | rule 142-143 (no numeric speedup without device measurement); 7 legs 148-162 as rows; matrix 164-167 one line; rule 167-171 (rotation upload keeps open-then-append shape; do not normalise away) | 14 |
| EEPROM CLEAN agreement device gate | 173-209 | 37 | T 60 / K 30 / D 10 | scope (RUN/DVDD reset & power cut deliberately not legs); entry (identical non-default layout+macro; High DH x2 + HP x2; DH x2 at Low after High); ordered pass criteria 181-195 as a numbered list; host-regression legs 197-209 as a leg table (they are coverage the device run must not manufacture) | 18 |
| Refactor Self-Check | 210-252 | 43 | K 50 / D 50 | tier table 216-220; "tier is a result" one line; T1/T2 outrank device readings, never waived; T1 build-day rule (3 bytes from `QMK_BUILDDATE` in `via_eeprom_set_valid()`, `quantum/via.c`; pin `--build-date` or accept exactly those 3 bytes; device fact: new-day first boot resets VIA region); rules 247-251. Delete 223-229 inlining stories | 20 |
| Source Gate | 253-311 | 59 | T 60 / P 20 / D 20 | 10 bullets -> check rows. Keep: matrix.c command + "never compare against another ERA branch"; absence list; resolved/compiled tuple rule; `usb_*`/`is_keyboard_master_impl` pair; `startup_connect`; no-success-before-capacity; DUAL-HOST eligibility 3-part check (2 AND sites + static assert). Pointer rows for 273-280 (storage contract) and 292-298 (authority contract). Delete 259-261, 262-267 (fork ledger owns `c93ef27143`) | 24 |
| ELF And Static-Capacity Checks | 312-399 | 88 | K 35 / T 30 / P 15 / D 20 | `era_residency_gate.sh <elf>` = 3 enforced checks, run by launcher per UF2; vector rule (all `.vectors` SRAM except reset slot; read bytes not names; `era_unhandled_vector` ASSERT is complement) - canonical here per `era_invariants.md:243`; `.map` stub rule (2 expected in `.flash_startup`, 1 in `.sram_image`, a new `.flash_startup` stub = pre-copy caller); 349-398 -> table check x instrument; core1 stack: `era_core1_stack_walk.py <elf>`, headroom is the acceptance, never hand-walk; `DUAL_RUNTIME_BUNDLE` permanent absence; eligibility procedure (`nm g_era_split_wire_section_eligibility` -> `objdump -s -j .sram_image`, 10 bytes in `era_split_mode_planner.h` order, expected `00 00 / C5 FC / C5 FC / FE F7 / FE F7`, re-derive from the five `ELIGIBLE_*` masks in the same commit); anchor seat zero permanently; static cap by equality; frame sizes. Delete 333-336 story, 380-389 history | 32 |
| Layout Checks | 400-427 | 28 | K 70 / P 30 | 5 bullets as rows: no layout change during functional change; gate per keymap ELF; objdump/nm list 408-412; magic equalities (`magic_location == __bss_end__ == __era_bootloader_magic_base__`, size 4; `early_hardware_init_pre()` in `system/era_boot_core1_halt.c` no `.sram_image` call; only 2 crt0 veneers) - the *why* 414-418 is residency contract's; smoke + fresh no-cable qwin point | 16 |
| Fixed Baselines | 428-468 | 41 | T 60 / K 30 / D 10 | one table baseline x value x device grounds x instrument: settle 128 (32/64 -> false input); USART 460800 (576000/748800 -> ACK gaps); floor 18 kHz (owner 2026-08-01), lowest cabled 21463; `smp_hz=80129` vs 80128, 12.48 us, `ovr=0 rearm=0 fd1=0`; chunk `c + LEDs x e`, c~8-11 us, e~2.97 / 14.9 us, four chunks (six-fold at three), fixed term in `quantum/rgb_matrix/rgb_matrix.c`; 62.34 vs 62.50 fps, animation speed unaffected; rule 467 | 16 |
| What the shipped firmware measures, on TOMAK79H | 469-528 | 60 | K 45 / T 25 / D 30 | window spec (80-110 s, 2 per config, flat); table 473-479 verbatim; owner decision HP baseline / DH option, HP outranks DH; rows: 0.415 us (2.28 %) / 0.438 / 0.497, responder > initiator (twice); cabled repro 0.075 vs 0.005 -> rule "twice, two sittings, before believing <0.1 us"; RIGHT -1.16 % = 55 vs 41 LEDs (+0.18 pred / +0.21 obs); "X %" meaningless without its pass (18 us vs 44.9 us); core1 sleep 84.0 / 89.1 %; PIO consumer 3.18 us; stack 912 B / 992 B vs 2048 B -> 1056 B headroom, per variant, re-run walk on call-depth change | 28 |
| The Split-Relation Cost Gates | 529-576 | 48 | K 45 / D 55 | table 534-537; rules: per half never pair-averaged; ratio not absolute (ceiling ~20 kHz); below 18.95 kHz ceiling ratio stops binding, floor catches; "nobody knows whether 5 % is large" (owner); owner decision: RGB-on-core0 rule withdrawn -> core0 owns switch->HID, core1 owns presentation; 3 non-licences; reopening = owner decision. Delete essays 544-553, 564-575 beyond those lines | 16 |
| Evidence Storage | 577-584 | 8 | D 75 / K 25 | one line: a new measurement replaces the figure in Fixed Baselines, never a series (raw-log rule is AGENTS.md's) | 2 |

**Totals: 584 -> ~270 lines** (incl. ~13 blank separators; -54 %).

**MUST-SURVIVE (gates):**
- REFUSED blocks: **none** in this file.
- Gates x pass criterion: the 9 What-A-Change-Owes rows (57-69); 12 host-test commands (79-90); scheduler gate criteria (127-132); NVM 7 legs + `program_failure_count`/`erase_failure_count`=0 (148-162); CLEAN phase order + `cq`/`hs` unchanged from event 4 + MAGIC_OFF + 7 domains MATCH `xfer=0 apply=0` + `vis=0 pnd=0` (181-195); host legs (197-209); T1 `uf2_sha256` / T2 `nm -S` set+sizes / T3 explained delta; Source Gate 10 items; residency gate 3 checks (32 KiB free-ram0, allocator absence, vectors); stub rule; stack walk vs reservation; eligibility bytes; anchor seat zero; static cap equality; Layout 5 checks incl. vectors LMA `0x10000100`; Floor >=18 kHz / Target >=95 % cabled / same-image no-cable, per half; 18.95 kHz ratio limit.
- Standing figures: 128 cycles; 460800; 21463; 80129/80128/12.48 us; 8-11 / 2.97 / 14.9 us, four chunks; 62.34/62.50; TOMAK79H rows 56,190/17.794 - 55,516/18.003 - 54,924/18.210/97.72 % - 54,841/18.233/97.59 % - 54,052/18.499/97.32 %; margins x3.0 and >=2.3 pts; 0.415 (2.28 %)/0.438/0.497; 0.075 vs 0.005; 1.16 % (55 vs 41; 0.18/0.21); 0.31-0.45 us = 1.5-2.2 % vs 0.7-1.0 % of 44.9 us; 84.0 %/89.1 %; 3.18 us; 912/992/2048/1056 B; 32-byte exception frame; T1 three-byte day diff.
- Owner decisions: floor+target both, 2026-08-01 (443, 531-532); HP baseline / DH option (481-482); RGB core0 rule withdrawn, reopening needs an owner decision (564-575); 5 % margin unknown (556-558).
- Device-established facts to keep as one line each: 32/64 false input; 576000/748800 ACK gaps; new build day resets VIA region (242-243); responder pays more (recorded twice); cabled 10x worse repro; RIGHT/LEFT = LED count; chunk fixed term does not shrink; 0.26 % residue not animation speed. Stories to delete but whose rule stays: vector check "caught two adoptions" (335-336), eligibility line wrong until 2026-08-17 (380-389), inlining moved RAM twice (225-229).

**ROUTED HEADINGS (gates) - keep exact text:**
- `What A Change Owes` <- `era_hid_report_contract.md:374` (Korean particle form, not regex-caught), `AGENTS.md:96` (prose).
- `Fixed Baselines` <- `era_route_contract.md:221-222` (bold, line-wrapped - regex misses the wrap), `era_walkthrough.md:24` (paren form, **regex-checked**).
- `Source Gate` <- `era_active_index.md:112`, `era_authority_contract.md:29-30`, `era_qmk_fork_ledger.md:121`, `era_hid_report_contract.md:377`, `AGENTS.md:99`.
- `Layout Checks` <- `era_active_index.md:104`, `.claude/rules/era-boot-layout.md:17` (bold), `era_hid_report_contract.md:377`. **Name drift:** `era_sram_residency_contract.md:215,380` say "Layout Gate"; gates' own table (62) says "Layout checks". Pick one.
- `Refactor Self-Check` + tier names T1/T2/T3 <- `era_feature_path.md:104-106`, `AGENTS.md:100`.
- Content routed without a heading name (keep the content, name is free): instrument switch-on (`era_build_and_flash.md:146`, `.claude/rules/era-build-gates.md:14-15`); NVM timing legs journal-room / rotation / 16-KiB macro close (`era_build_and_flash.md:193-194`, `era_capture_reading.md:577`, `era_sram_residency_contract.md:336`); `.vectors` gate + ASSERT (`era_invariants.md:242-243`, `era_sram_residency_contract.md:186`); eligibility bytes (`era_closed_surface_contract.md:17`, `era_wire_contract.md:376`, `era_active_index.md:93`); manual objdump/nm review (`era_sram_residency_contract.md:117`); 3 copy-to-RAM pass bands (`era_board_adoption.md:118`, `era_active_index.md:107`); `era_via_exact_ms` host transcript proof (`era_active_index.md:97`); margin at the poll period (`era_invariants.md:73`); 18 kHz per-half floor (`era_overview.md:174`).
- Note: `era_capture_reading.md:577` expects "the actual NVM width" as a device measurement *in* this file; no such figure exists here (the NVM gate only demands one). Either land the figure or fix that pointer.

**CHECKER TRAPS (gates):**
- `foo()` + verb lines: 235 (`via_eeprom_set_valid()`, file `quantum/via.c` same paragraph), 356 (`era_split_communication_core_entry()`, file at 357), 421 (`early_hardware_init_pre()`, file inline), 454 (`rgb_matrix_indicators_advanced()`, file inline). All safe today; a table is one paragraph to the checker (split on blank lines), so keep name and file in the same row/paragraph after restructuring. `is_keyboard_master_impl` (287) has no `()` and is not a claim.
- `2026-` lines: 261 report/delete; 381 report/delete; 383 report/delete; 384 report/delete; 443 rule-evidence/keep; 532 rule-evidence/keep (same decision as 443 - state once).
- Constant arm: no `ERA_|RGB_MATRIX_|MATRIX_|RP2040_` name is followed by a number on the same line here; nothing to preserve.

**Duplication (gates -> canonical owner):** 32 KiB floor (319) -> `era_sram_residency_contract.md` **Verification** (:28,37,367); `c93ef27143` + six-roots essay (262-267) -> `era_qmk_fork_ledger.md:10-20`; matrix.c departure list (255-258) -> `era_invariants.md`; replacement Apply bullet (273-280) -> `era_host_peer_storage_contract.md`; Core1/Core0 ownership (292-298) -> `era_authority_contract.md`/`era_invariants.md`; magic-word placement mechanism (413-418) -> `era_sram_residency_contract.md:199-215`; variant/selector mechanics (20-36) -> `era_build_options.md` + `era_build_and_flash.md`; raw-log rule (579-581) -> `AGENTS.md`.

### A5-b. `era_build_options.md` - 484 lines

| Heading | Lines | N | Verdict | What survives | After |
|---|---|---|---|---|---|
| title + header + pointer | 1-14 | 14 | K | header lines; 12-13 pointers | 7 |
| Where a build option is declared | 15-135 | 121 | K 40 / T 35 / D 25 | layer table (make / `config.h` / `keyboard.json` / `<board>.h` x what it can decide, 23-38); rules **1-5** numbered, <=2 lines each (rule 2 exception `ERA_STORAGE_QUIET_DEFER_MS`; rule 4 worked case `rgb_matrix.led_process_limit`); tie-break -> rule 1; one option/one declaration/one file `keyboards/era/era_build_options.mk`; `era_common_qmk_rules.mk` sole owner of `DEBOUNCE_TYPE`; three REFUSED blocks 93-105, 107-121, 123-134 **verbatim**. Delete 17-21 framing (keep "owner decision"), 59-67, 81-87, 89-91 stories | 72 |
| Why `<board>.h` is not a declaration site | 136-154 | 19 | D 80 / K 20 | fold into the `<board>.h` layer row: "derived macros (`ERA_TAP_DANCE_ENABLE`) are emitted from `era_tapdance_rules.mk`, never a board header". Heading not routed to; can go | 2 |
| The three shapes the `_ENABLE` suffix still names | 155-166 | 12 | T | 3-row table shape x rule x form | 6 |
| The selectors | 167-259 | 93 | K 40 / T 30 / D 30 | preamble -> 5 lines (firmware selectors in `era_build_options.mk`; `ERA_BUILD_VARIANT`/`ERA_SHOW_OPTIONS`/`ERA_BUILD_IDENTITY_REPORT` in `era_build_identity_options.mk` for Brick65; fragment includes declaration file first; board assigns above include and wins; variant axes `override :=`). Table 185-207: every row kept, cells cut to selector - default - refused-with (by name) - one-line meaning; keep rule-5 facts `RP_DMA_REQUIRED`, `ERA_MOUSEKEY_RUNTIME_DELTA`; owner decision "no second raw backend, no off state" (189); 4.13 us/pass -14.3 % (205); `ERA_STORAGE_QUIET_DEFER_MS` row = 500 + grep command, story deleted. Tuple table 209-218 verbatim; 220-229 -> 3 lines (identity handshake; one ELF witness per axis, stale = zero-storage absolute symbol in `split/era_split_transaction_engine.c`; overrides are test inputs not a seventh combination); 231-236 -> 4 rows name-default-file; 238-246 -> 2 rows; 247-251 one line; 253-258 one rule line | 62 |
| The values whose default lives in C | 260-277 | 18 | T | 7 knobs x file (2 in `era_split_wire_protocol.h`, 5 in `scheduler/era_split_transport_scheduler_internal.h`); rule: empty `?=` in manifest, default in C. Heading self-routed from 207 - keep text | 11 |
| The VIA surface and its gate | 278-303 | 26 | K 30 / P 70 | owner decision (no VIA -> no ERA VIA code); gate `ifeq ($(strip $(VIA_ENABLE)), yes)` in the 3 fragments, post_rules phase only; `ERA_VIA_BOOTLOADER_ENABLE`/`ERA_EEPROM_CLEAN_ENABLE` read only in `era_via_system.c` -> `-D` under the gate; headers ungated on purpose. Closure narrative 287-293 -> pointer | 7 |
| Where the options live, and how to see what a build used | 304-367 | 64 | T 55 / D 45 | file table 306-321 with trimmed cells (delete 310's count story; keep 320's owner decision); `?=` declares / board `=` wins (2 lines); `era-build --show-options <board>:<keymap>` -> option-value-origin (`file`/`command line`/`environment`); manifest fields `requested_variant=`, `variant=`, `resolved_tuple=`, `compiled_tuple=`, command; scope: make-declared only, ~40 rule-2 `#ifndef` values outside it, `grep -rn "#ifndef ERA_" keyboards/era`; rule "a derived set's predicate is one a new member satisfies by existing". Delete 347-356 story | 30 |
| The dependencies, and which layer each can be declared in | 368-439 | 72 | T 55 / K 15 / D 30 | phase table with `builddefs/build_keyboard.mk` cites `:101-105/:143/:146/:445-449/:473/:494-495`; rule: wrong phase is refused by `era_common_qmk_rules.mk`; dependency table 394-409 kept as edge - where - form, cells trimmed (keep `LAYER_STATE_8BIT` = wire-format fact + `#error` on `DYNAMIC_KEYMAP_LAYER_COUNT` in `era_split_peer_layer.c`; `NO_USB_STARTUP_CHECK` reason <=1 line; ALWAYS_ON vs EFFECT = two claims about one pin). Delete 381-388 upstream examples, 411-424 narrative beyond the two facts, 426-438 tomak79s story (one line max) | 34 |
| What a build combination is expected to do | 440-485 | 46 | K 40 / T 20 / D 40 | property 1: 44 combinations (22 RP2040 x `default`/`via`) build + pass 3 copy-to-RAM gates; `default` = all but VIA surface; property 2: every declared off state builds, sole exception `ERA_TAPPING_CONFIG_ENABLE=no` on split (refused by name pre-compile); 4 failure shapes as a 4-row table; 4 `sirind/tomak` layout keymaps (names, 94/95/95/96, derived from `default` filtered by each layout's `matrix` list - re-derive, never hand-edit). Delete 482-484 story | 16 |

**Totals: 484 -> ~260 lines** (incl. ~11 blank separators; -46 %; 42 of those are the three REFUSED blocks).

**MUST-SURVIVE (options):**
- REFUSED (verbatim, 3 lines each): 93 "move the declarations into `config.h`, where a stock QMK keyboard puts build-time configuration."; 107 "declare them in `keyboard.json`."; 123 "adopt `post_config.h` for the derived defaults and the dependency checks." (WHY lines carry the evidence figures 63/45/18/27/13/14 SRC lines, ~38 QMK switches, `build_keyboard.mk:143`, `:537`, `common_rules.mk:275`.)
- Selector defaults (all verified = tree): `ERA_BUILD_VARIANT` standard; `ERA_BOARD_COMMON_ENABLE` yes; `ERA_RP2040_MATRIX_ENABLE` yes; `ERA_RP2040_MATRIX_GPIO_INPUT_PIN_DELAY` 128; SOCD/KKUK/DEBOUNCE/TAPPING_CONFIG yes; `ERA_MOUSEKEY_CONFIG_ENABLE` yes (VIA channel 13); `TAP_DANCE_ENABLE` yes; `ERA_BACKLIGHT_EFFECT_ENABLE` no (ids 0..3); `ERA_BACKLIGHT_ALWAYS_ON` no; `ERA_RGB_INDICATOR_ENABLE` no (ids 6..12; `ERA_RGB_INDICATOR_1_LED`/`_2_LED`); `ERA_VIA_BOOTLOADER_ENABLE`/`ERA_EEPROM_CLEAN_ENABLE` yes; `ERA_STORAGE_QUIET_DEFER_MS` 500; the 4 `RGB_MATRIX_*` policy switches yes; `RGB_MATRIX_IDLE_GATE_ENABLE` yes (`RGB_MATRIX_IDLE_GATE_US` 1000); `ERA_SPLIT_SERIAL_USART_SPEED` 460800 (Medium/Low = /2 / /4; scale = ceil(speed/baud); `#error` in `era_split_via_link.c`); `ERA_SPLIT_EEPROM_SYNC_ENABLE` no (TOMAK_TKL/79H/79S yes); `ERA_SPLIT_RP_USB_SLEEP_SYNC` 1; the 5 diagnostic axes no; 7 timing knobs empty; rule-2 knobs 64 / 4 / 10000 / 0; `ERA_SPLIT_COMMUNICATION_CORE_STAGE` = `CORE1_FULL` only.
- Tuples (axes `wire,qwin,phase,cause,stale`): standard `no,no,no,no,no`; wire `yes,no,no,no,no`; qwin `no,yes,no,no,no`; cause `yes,no,no,yes,no`; stale `yes,no,no,no,yes`; qwin_phase `no,yes,yes,no,no`.
- Owner decisions: declaration rule (commissioned three-arrangement judgement, 17-21); no second raw backend / CPU bit-bang tuning = duplicated investment (189); no-VIA build links no ERA VIA code (280); each board `post_rules.mk` states its feature set explicitly (320).
- Measured facts: 4.13 us/pass, -14.3 % phase instrument (205); 128 rejected downward on device (190, grounds in gates).
- Every refusal-by-name in the table and dependency rows (the `$(error)`/`#error` sites) - these are the gate this manual owns.

**ROUTED HEADINGS (options):** no bold-heading regex form routes into this file; routes are by content and by **rule number**:
- "rule 3" <- `era_board_adoption.md:458` - **keep rules numbered 1-5 with the same meanings**.
- "the three arrangements tried and rejected" <- `era_feature_path.md:59-60`, `era_active_index.md:111`.
- immutable tuple <- `era_invariants.md:151-152`, `era_overview.md:187` ("which rungs exist"), `era_active_index.md:111`.
- "why there is no second raw backend, and no selector offering one" <- `era_source_map.md:140`, `era_board_adoption.md:201`.
- "every selector, what each costs, where declared" <- `era_build_and_flash.md:114-115`, `era_source_map.md:370-371`, `era_board_adoption.md:14`, `.claude/rules/era-build-gates.md:10-11`.
- Self-route 207 -> **The values whose default lives in C** (keep text). Outbound 319 -> **Storage Adoption** exists in `era_board_adoption.md:16` and `era_source_map.md:118`.

**CHECKER TRAPS (options):**
- `foo()` + verb lines: **195** `backlight_init()` "reads" - the *row* names no `.c/.h/.mk` file; it passes only because the whole table 185-207 is one paragraph containing other file names. If that row becomes its own paragraph it fails. 200 `rgb_matrix_task()` (file inline, safe); 291-292 `era_split_keyboard_handle_via_command()`/`via_custom_value_command_kb()` "calls" (paragraph names `era_common_via.c` etc., safe); 401, 465 `debounce()` - no listed verb, safe.
- `2026-` lines: 198 report/delete; 310 report/delete.
- Constant arm (name followed by a number within 90 chars on the same line, x1000 tolerance): 190 (128), 198 (500), 200 (1000), 201 (460800), 203 (1), 233-235 (64, 4, 10000, 0). Keep each value on the same line, after the name.
- Citations checked for range: `builddefs/common_rules.mk:275` (30, 139), `build_keyboard.mk:143` (115), `builddefs/build_keyboard.mk:537` (131), `era_split_qmk_rules.mk:16-28` (240). The bare `:101-105` etc. at 373-376 are not regex-caught; write them as `file:line` if kept.
- **Stale claim found:** 198 says `system/era_board_hooks.c` "is the only" ERA reader of `ERA_STORAGE_QUIET_DEFER_MS`; `git grep -l` today also returns `keyboards/era/common/storage/era_nvm.c` (plus `rgb_matrix.h`/`rgblight.h` beyond the four core files listed). The cell is wrong a third time - keep only the command.

**Duplication (options -> canonical owner):** post_rules-after-keymap-rules fact (370-379) also in `era_active_index.md:111` and `era_common_qmk_rules.mk:7-22` comments - keep the cites here, index is a router; `RP_DMA_REQUIRED` (189) <-> `era_board_adoption.md:184`; QUIET_DEFER core-file list (198) -> `era_qmk_fork_ledger.md:44-54`; storage-adoption include (319) -> `era_board_adoption.md` **Storage Adoption**; tomak79s indicator / `tomak_common.c` (426-438) -> `era_board_adoption.md:369` (board content); board `config.h` contents (rule 3) <-> `era_board_adoption.md:458`; 128 baseline sentence (190) -> gates **Fixed Baselines**. The layout-variant keymaps (476-484) and the 44-combination count have no other home - keep here.

---
## A6. `contracts/era_host_peer_storage_contract.md` (610 lines, worktree) and `contracts/era_sram_residency_contract.md` (383 lines, worktree)

Uncommitted state at audit time: storage contract HEAD 578 -> worktree 610 lines (+38/-7: file names added beside `era_nvm_format()`, `era_nvm_replace()`, `era_eeprom_driver_prepare_reboot_word()`; three new Recency paragraphs at 266-282; the ready-result paragraph 552-564 rewritten); SRAM contract HEAD 381 -> 383 (`storage/era_nvm.c` added at 313-314). Neither document contains a `2026-` date. Every constant spot-checked against source (via `git grep`) matched - list in A6-c. **Re-check both after the NVM work lands; line numbers below are the worktree's at audit time.**

### A6-a. `era_host_peer_storage_contract.md` - 610 lines

| Heading | Lines | N | Verdict | What survives | After |
|---|---|---|---|---|---|
| Title + header + preamble | 1-13 | 13 | K header / D 11-13 | header lines (two after Phase 3); preamble ("Git carries the retired history...") deleted | 7 |
| Scope And Authority | 15-57 | 43 | T 40 / P 30 / K 30 | stack as a 5-row table (layer, owner, file); adapter duties one row; K: logical EEPROM = `EEPROM_SIZE` 24576, `EEPROM_DRIVER=custom`, no wear-leveling, void QMK write -> result-bearing API below it | 14 |
| ERA NVM Physical Contract | 59-94 | 36 | T 50 / K 50 | physical table (128 KiB at end of 2 MiB; bank A/B 64 KiB; header 0x0000 / activation 0x0100 / snapshot 0x0200 / journal 0x6200 per `era_nvm_format.h`; page 256; sector 4 KiB; no 64-KiB block erase); rules: newest valid bank is authority, incomplete never displaces, old-or-new (3 bullets), append failure seals, readback verify, `ERA_NVM_FORMAT_VERSION` != `ERA_EEPROM_RESET_KEY` | 20 |
| Current Storage Inventory | 96-125 | 30 | K | domain table as is; geometry examples as 2 rows; config block split (256 @ 37: 176 portable, 176..255 local; owner map in `storage/era_eeprom_layout.h`; RESET_KEY policy) | 22 |
| Excluded Local State | 127-140 | 14 | K | the 7-item list + 1 rule line | 9 |
| Normal Local Writes And Dirty Publication | 142-175 | 34 | K 60 / P 20 / D 20 | notification below QMK core, no `nvm_*` hook; macro-staging exception (bold rule 152-154); two consumers; 1000 ms (`ERA_HOST_PEER_STORAGE_DIRTY_QUIET_MS`), deadline replace, immutable settle capture, no idle patrol; 500 ms coalescing -> one owner-policy line + pointer to `era_qmk_fork_ledger.md` (`ERA_STORAGE_QUIET_DEFER_MS`) | 14 |
| Dynamic Macro Transaction | 177-209 | 33 | T 60 / K 40 | transcript line; marker semantics; 6-step table (step, public image, durable record, revision, indicator); asymmetry rule 205-209 (2 lines) | 18 |
| Stock QMK macro RESET transcript is a bound dependency (###) | 211-232 | 22 | K 70 / P 30 | 16-byte sequential transcript rule, `ERA_NVM_QMK_MACRO_RESET_CHUNK_BYTES` 16, `quantum/nvm/eeprom/nvm_dynamic_keymap.c` must keep the loop, `tests/era_nvm_qmk_driver` compiles the stock file; interleaved read aborts; aligned-all-zero alternative -> REFUSED form | 9 |
| External H7S State-Sync And Macro Handoff | 234-257 | 24 | T 60 / K 40 | byte-map table + revision-meaning table + "not copied" list | 16 |
| Recency Layer | 259-294 | 36 | T 40 / K 60 | record table (baseline CRC 220..251 = 32 B; counters in local policy block -> `era_authority_contract.md` owns that layout); compare table (equal / different / invalid); rules 266-275, 280-282, 291-294 one line each | 18 |
| Arbitration | 296-315 | 20 | K | table + larger counter wins, tie -> Left, direction one round, verify-all at relation start, news edge arms summary only | 12 |
| Storage News Value And Relation-Open Audit | 317-328 | 12 | K | news value = forward-only 7-bit, zero = no claim, summary decides (3 lines); indicator composition 324-328 -> move under **Diagnostics** (two routes aim there, see A3) | 6 |
| Relation Admission | 330-340 | 11 | K | one engine for HP/DH; admission inputs; responder admitted to answer a policy refusal; initiator needs local policy requested; eligibility pointer | 5 |
| Source Revision And Identity | 342-355 | 14 | K | revision != CRC; fence set as one line; gen 0 invalid; 16-bit gen unique per relation; wrap rotates + invalidates manifests | 7 |
| Fixed Wire Contract | 357-367 | 11 | P 60 / D 40 | one pointer to `era_wire_contract.md` + the core0/core1 split line; "Session 2 cutover" sentence deleted | 2 |
| Transaction State Machine (+ ### Pull, ### Push Transaction) | 369-401 | 33 | T 70 / K 30 | pull chain as ordered table (step, actor, abort exit); push 3 lines; "direction changes wire roles, never NVM semantics" | 20 |
| Replacement Apply: ADMIT And Public Authority | 403-441 | 39 | T 40 / K 60 | 9-row precondition table; rules: ADMIT final boundary, post-ADMIT not rollback authority, public range after commit only, failure -> no manifest/revision/COMPLETE, NVM canonical -> repair forward, `REMOTE_APPLY` origin is not a local dirty edit | 20 |
| Retry, Duplicate, And Failure Semantics | 443-462 | 20 | T 70 / K 30 | failure-class table (5 rows) + wire retry line + `program_failure_count == 0`, `erase_failure_count == 0` | 12 |
| Inactive-Bank Maintenance And Rotation | 464-485 | 22 | K 50 / P 50 | one 4-KiB sector per `era_eeprom_driver_maintenance_task()` call from `era_common_features_task()`; no recursion; mandatory rotation synchronous and device-measured; macro-upload rotation shape optimisation needs device evidence; **house the SRAM REFUSED block here** (see A6-a 5) | 10 |
| Why An EEPROM Clean Is An Agreed Restart | 487-531 | 45 | K 60 / T 20 / P 20 | the five "why"s the index promises, one line each; PREPARE = one replay-proved `EECONFIG_MAGIC_NUMBER_OFF` write via `era_eeprom_driver_prepare_reboot_word()`; order chain as table; failed prepare sticky; boot path (519-524) one pointer line; power-loss boundary 2 lines | 24 |
| Capacity And Publication | 533-564 | 32 | K 50 / T 20 / D 30 | figures table: 24-KiB image, one `ERA_HOST_PEER_STORAGE_IMAGE_BYTES` 16384 buffer, no old-image copy, `ERA_HOST_PEER_STORAGE_CORE0_STATE_BYTES` 140, `ERA_HOST_PEER_STORAGE_STATIC_BUDGET_BYTES` 18524 (equality asserts, re-read at landing - header is under edit); seqlock rule; ready-result rule 3 lines (discard before unserviced return; same-role keeps lease); consequence narrative 560-564 deleted | 12 |
| Scheduler And Matrix Recovery | 566-578 | 13 | K | 4 rules | 4 |
| Diagnostics | 580-598 | 19 | T 50 / P 30 / K 20 | acceptance table (5 rows); counter decode -> pointer to `era_capture_reading.md`; receives the pending-fact composition from 324-328 | 10 |
| What The Lane Costs A Typist | 600-609 | 10 | K | 3 lines | 4 |

**Totals: 610 -> ~310** (297 content + headings/blanks).

**2. MUST-SURVIVE**
- REFUSED blocks: **none** in this document (the SRAM one moves here, see 5).
- Values: 24 KiB / `24576` (`EEPROM_SIZE`); 2 MiB flash; 128 KiB region = 2 x 64 KiB banks; 24-KiB snapshot; 256-B page; 4-KiB sector; `ERA_NVM_FORMAT_VERSION`, `ERA_EEPROM_RESET_KEY`; domain table ids 0-6 with ranges/bytes 176 / geometry / 16384 / 8 / 2 / 1 / 1, schema 1; keymap base 297, macro follows; offsets 23 / 4 / 3 / 296; 12x9 -> 864 B `297..1160`, macro `1161..17544`; 12x11 -> 1056 B, macro 1353; config block 256 @ 37, portable 176, local `176..255`; baseline `220..251`; 1000 ms quiet; 500 ms coalescing; marker = final byte of 16 KiB; `ERA_NVM_QMK_MACRO_RESET_CHUNK_BYTES` 16; 32-byte RAW HID; 16-bit divergence counter, `UINT16_MAX`; 7-bit news; 32-bit source revision; 16-bit generation; one 4-KiB sector per call; 16-KiB transfer buffer.
- H7S envelope (exact, lines 239-248): `GET_KEYBOARD_VALUE` `0x02`, selector `0x06`, version `0x01`; request byte 3 and `6..31` zero; bytes `4..5` opaque echoed tag; OK = `0x00` in byte 3; byte 6 = mask `0x07`, byte 7 zero; bytes 8/12/16 = nonzero big-endian 32-bit KEYMAP/MACRO/CONFIG; `20..31` zero. Revisions: KEYMAP = keymap/encoder image changed; MACRO = durably completed upload or standalone RESET only; CONFIG = GET-visible value changed (SET-before-SAVE, independent durable local write, or remote-domain publication); nonzero wrapping RAM generations, not NVM epochs. **H7S must not copy**: ERA NVM banks, split authority, replacement Apply internals, RP2040 flash policy; no new command id / report / transaction id / capability bit / BUSY state.
- Owner decisions (one line each): 500-ms coalescing is accepted durability/endurance policy, not a second engine (170-175); tie -> Left (308); aligned-all-zero RESET recognition is not production (230-232); macro-upload rotation shape not optimised without device evidence (482-485); mandatory-rotation width is a device measurement (476-480); CLEAN is a two-half operation (489-493); no simultaneous-reset promise under RUN/DVDD removal (525-531).
- Device-established facts grounding rules: keyboard-originated writes during an upload (deferred RGB flush) stay durable (205-209); a deferred ready-result retirement produces a permanent timeout loop or housekeeping wake (560-564, keep as half a line).
- Headline rules: old-or-new never partial (80-85); public RAM only after durable commit; void QMK write -> result-bearing below (55-57); opening/staging a macro is not a committed-span notification (152-156); macro transaction gates only writes touching its domain; invalid baseline conservative across boot (266-269); metadata writes create no revision/notification (277-282); direction valid one round (309-311); verify-all at every relation start (313); hint = ask, summary decides (319-322); ADMIT final cancellation boundary (405); post-ADMIT change is not rollback authority (420-423); NVM canonical -> repair forward (433-437); failure classes separate (449-458); no keyboard/wire/scheduler work inside a primitive (471-474); both halves quarantine and replay-prove PREPARED before any deadline (489-501); failed prepare sticky (515-517); budgets are compile-time equalities (539-541); source image never mutated while core1 may copy (543-545); ready result is a core0 wake fact (547-559); exclusivity ends at validated candidate (568-571); never roll bytes back for relation recovery (577-578); typing is not a storage poll (602).

**3. ROUTED HEADINGS (keep exact text)**
- **Arbitration** <- `era_authority_contract.md:220`, `:260` (paren form), `era_route_contract.md:707`.
- **Diagnostics** <- `era_closed_surface_contract.md:125`, `era_wire_contract.md:232` - both for "the pending fact's composition and its consumer (the lamp's mirror arm)", which currently sits at 324-328 under Storage News Value; move it under Diagnostics.
- **Why An EEPROM Clean Is An Agreed Restart** <- `era_active_index.md:100` (and the row lists the five "why"s that must stay there).
- **What The Lane Costs A Typist** <- `era_walkthrough.md:183`.
- Unchecked-form routes (renaming silently breaks them): **Recency Layer** <- `era_authority_contract.md:243` "(Recency Layer)"; **Relation Admission** <- `era_authority_contract.md:295`; **Capacity And Publication** <- `era_capture_reading.md:341-342` (paren form wrapped across two lines).
- Routes by description: `era_active_index.md:97` (envelope, revision meanings, marker transcript, compatibility boundary, what H7S must not copy); `era_wire_contract.md:49-50` (domain table, state machine, capacity, retry, durable-apply rules); `era_qmk_fork_ledger.md:86-87` (stock-RESET dependency); `era_source_map.md:97` (domains, exclusions, ranges, sizes, reloads).
- **Dangling routes into this document** (claimed content it does not hold): `era_route_contract.md:250-252` says the `ERA_HOST_PEER_STORAGE_PEER_SILENCE_MS / RETRY_MS` derivation is here - neither name appears (defined `split/era_host_peer_storage.h:125` = 25U, `:149` = 2 x `PEER_STALL_WORST_MS` 500U); `era_wire_contract.md:120-123` says what `ERA_HOST_PEER_STORAGE_V1_ENABLE` must compile in is here - absent, and a `#define` grep found no such macro (verify); `era_performance_gates.md:394-396` says "the cap's value" is here - the capacity section names no identifier or value (add the two budget constants above, number on the same line).

**4. CHECKER TRAPS**
- Currently no failing paragraph. Paragraphs that pass only because the file is named elsewhere in the same paragraph and will fail if split into separate rows: 44-53, 213-220, 222-227, 389-398, 466-469, 495-501, 519-524, 547-564.
- 425-428: `era_nvm_replace()` with "publishes" (not a listed verb) - add `storage/era_nvm.c` when rewriting, or any listed verb trips it.
- Constant check reaches only `ERA_|RGB_MATRIX_|MATRIX_|RP2040_`-prefixed names with a number after them on the same line: today only `ERA_NVM_QMK_MACRO_RESET_CHUNK_BYTES` is 16 (222) is checked. Write new tables as "`NAME` value" on one line so drift is caught.
- Dates: none.

**5. Duplication (canonical owner elsewhere)**
- Inactive-bank maintenance: 464-485 == SRAM 266-270 + 328-337 == `era_invariants.md:347-353` - three copies; one owner (this document) and the REFUSED block moves with the rule; SRAM keeps a pointer.
- ADMIT / old-or-new / repair-forward: 403-441 == SRAM 313-326 == `era_invariants.md:139-148` (routes here for the exact order) == `era_performance_gates.md:273-279` == `era_source_map.md:85` prose.
- Core0/core1 storage ownership 366-367 == `era_invariants.md`/`era_authority_contract.md`.
- CLEAN encoding: 502-513 vs `era_wire_contract.md:285-320, 556-570` (owns REQUEST/PREPARED/COMMIT_ARMED bits), `era_route_contract.md:60-66` (quarantine admission), `era_source_map.md:173` (10 s window, 500 ms restart quiet, `era_via_system_eeprom_invalidate()`). Keep only the "why" list here.
- 500 ms coalescing 170-175 == `era_qmk_fork_ledger.md:44, 50`.
- News value definition 319-322 == `era_overview.md:132` glossary, `era_wire_contract.md:756, 830-831` encoding, `era_route_contract.md:245-247, 560-561` semantics.
- Divergence counter placement 262-263: `era_authority_contract.md:240` owns the policy-block layout (`+10..+23`).
- 1000 ms quiet restated at `era_route_contract.md:248-249`, `era_wire_contract.md:244` (this document owns the constant).
- Diagnostics counters 580-586 == `era_capture_reading.md` (decode owner).
- Stack/adapter ownership 15-53 == `era_source_map.md` storage rows; `EEPROM_DRIVER=custom` bundle == `era_build_options.md` / `storage/era_storage_adoption_rules.mk:35`.

### A6-b. `era_sram_residency_contract.md` - 383 lines

| Heading | Lines | N | Verdict | What survives | After |
|---|---|---|---|---|---|
| Title + header | 1-12 | 12 | K | header lines (two after Phase 3) | 6 |
| Why The Image Is Resident | 14-29 | 16 | K 50 / D 50 | QSPI/XIP constraint (2 lines); reserves table (release profile ~24 KiB, XIP cache 16 KiB, LTO off 5-15 % + own gate); "no free-RAM table" meta deleted; floor pointer | 8 |
| Load Layout | 31-46 | 16 | T | regions table: flash contents; ram0 256 KiB striped, >=32 KiB free; ram4 `0x20040000` 4 KiB core0 stacks 3 KB + 288 B; ram5 `0x20041000` 4 KiB core1 stack 2048 + 48x4 = 2240 B via `.ram5_clear`; ram7 `0x20041f00` 256 B ROM scribble; ~1.6 KiB unclaimed | 12 |
| Ownership Does Not Move With Placement | 49-59 | 11 | P 70 / K 30 | bold rule "SRAM residency grants no new access" + pointer to invariants/authority | 3 |
| Scheduling Discipline | 61-74 | 14 | K / T | 4-row table; file ownership -> source map | 8 |
| Load Image Scope | 76-157 | 82 | T 35 / K 30 / P 25 / D 10 | pinned-TU table (6 TUs, selectors, `.rodata` arm); veneer set (2; third = pre-copy caller signature); ASSERT table (symbol -> bracket); rules: flash keeps only {boot2 `0x10000000`, `.vectors` LMA `0x10000100`, carve-out, `.sram_image` LMA, NVM region}; BOARD default or amend carve-out; closure manual (gates); copy via `CRT0_INIT_DATA`/`CRT0_INIT_VECTORS`; `rules_stacks_c1.ld` excluded (`CRT0_EXTRA_CORES_NUMBER=0`); accepted residual `CRT0_VTOR_INIT=1`. Deleted: injection-validation story 133-136, five-object enumeration 93-97 (ld:88-97 has it), "two jobs in one function" 105-109 (source map row + file top), +-2 KB branch 89-90 (ld selector rules) | 40 |
| Vector Table Residency | 159-193 | 35 | K 50 / P 20 / D 30 | all but index 1 in SRAM, index 1 flash by design; `system/era_vector_defaults.c` aliases (38 = 33 + 5 conditional, re-derive command); 16 B ram0; two complementary checks; attribution not recovery; `_unhandled_exception` shortcut -> REFUSED block; "carve-out is the cause" explanation deleted (ld:312-330) | 14 |
| Double-Tap Magic Placement | 195-231 | 37 | K 50 / D 30 / P 20 | placement table (section, after `.bss`, before `INCLUDE rules_memory.ld`, two ASSERTs, global symbol); dead zone: 2 lines (not measurable from firmware; 35-80 ms was arithmetic; tests bound to RUN-pin/cold boots) | 14 |
| Pre-copy reset guard layout (###) | 233-249 | 17 | K | rule list; 768 x 240 (`_Static_assert` == 184320 in `system/era_boot_core1_halt.c`) | 10 |
| Core1 Vector Path | 251-256 | 6 | P | one line naming `split/communication_core/era_split_communication_core_lifecycle_rp2040.c` (48 entries, 256-aligned, first-launch copy, TIMER_IRQ3 patch) | 2 |
| Flash Write Guards And Boot Ordering | 258-304 | 47 | P 40 / K 40 / D 20 | bullets 259-274 -> pointer to storage contract; boot-ordering invariant 275-278 (3 lines); launch site/policy split 280-296 -> pointer to `era_invariants.md:329-336` (verbatim there); "~86 lines" deleted; two crt0 ELF facts 297-304 (2 lines) | 12 |
| Apply-Liveness Design | 305-345 | 41 | K 40 / P 40 / REFUSED verbatim | no core1 stop across the NVM transaction; core0 keepalive forbidden (2 lines); rest -> pointer to storage; bank-maintenance -> pointer; REFUSED 339-345 verbatim (with the rule it protects - see A6-a 5: move with the rule to the storage contract, leave a pointer) | 12 |
| Closed Placement Surfaces | 347-361 | 15 | K | list + evidence-gated SRAM5 exception | 10 |
| Verification | 363-382 | 20 | K 60 / T 20 / D 20 | gate table (residency = ALLOC ram0 sections excl. `.heap`; floor 32768 of 262144; allocator symbols; `.vectors` 48 entries; ELF-only); runtime heap line; Layout Gate pointer; odessey60s story deleted (`common/tools/era_residency_gate.sh:15` keeps it) | 10 |

**Totals: 383 -> ~170.**

**2. MUST-SURVIVE**
- REFUSED (339-345): "> **REFUSED:** reintroduce recursive keyboard/wire work from inside ERA NVM to make a long rotation appear interruptible." - all three lines verbatim (one owner; see A6-a 5).
- Values: 2 MiB flash; 128-KiB NVM region; ram0 262,144 B, floor 32 KiB / 32768; ram4 `0x20040000` 4 KiB (3 KB + 288 B); ram5 `0x20041000` 4 KiB, 2240 B = 2048 + 192, `0x20041000..0x200418BF`; ram7 `0x20041f00` 256 B; ~1.6 KiB unclaimed; `.boot2` `0x10000000`; `.vectors` LMA `0x10000100`; six pinned TUs; two veneers `__main_veneer`/`____late_init_veneer` (`crt0_v6m.S:289`/`:306`); five `*board.o` matches; 38 `ERA_VECTOR_DEFAULT` (33 + 5); 16 B ram0; 48-entry 256-B-aligned core1 table; one 4-byte magic word; 768 x 240 ROSC ticks; `.bss` clear loop `crt0_v6m.S:267-281`; reserves 24 KiB / 16 KiB / 5-15 %; `CRT0_EXTRA_CORES_NUMBER=0`; `CRT0_VTOR_INIT=1`; `quantum/main.c:44`; allocator symbols `malloc`/`_sbrk`/`chCoreAlloc*`/`chHeapAlloc`.
- Owner decisions: closed placement surfaces list (350-358); SRAM5 migration evidence-gated (360-361); accepted VTOR residual (154-157); c1 stack reservation dropped (150-153); no fault recovery planned (191-193); dead zone not to be re-measured (219-231); vendor makefiles untouched (157).
- Device/ELF-established facts: odessey60s linked `malloc` via RGBLIGHT for ten days (grounds ELF-only gate); ASSERTs validated by three injected breaks (grounds "necessary, not closure proof"); `__init_ram_areas()` does not zero `.bss` (297-304); 35-80 ms was arithmetic; `NVIC_SystemReset()` resets only the requesting core -> dead zone is physical-reset-only.
- Headline rules: whole image executes from SRAM on both cores (32); flash keeps only the listed five things (84-87); BOARD default or amend carve-out (110-112); veneer set is the gate (120-121); closure is manual (114-117); every vector but index 1 in SRAM (161-164); magic word outside every copy/clear range (197-202); pre-copy TU may touch only reset-domain MMIO, ROSC `COUNT`, magic, stack locals, no call into `.sram_image` (238-243); mount + unique-ID read precede the first core1 launch, no earlier entry point (275-278); placement relaxes no scheduling rule (63); SRAM residency grants no new access (58-59); no core1 stop across the NVM transaction, core0 keepalive forbidden (307, 321-323); a relation failure during the call does not change NVM authority (324-326).

**3. ROUTED HEADINGS**
- No other document routes to an SRAM heading by name (all 21 route lines checked; the only heading-text hit, `era_source_map.md:207 ## Load Layout`, is that map's own heading). Internal self-routes: **Verification** (line 29), "the recorded 16 KiB reserve above" (355).
- File-level routes that name content this document must keep: `era_invariants.md:212` (magic placement asserted by name), `era_source_map.md:302-303` (scan-path consumption, task-boundary rule, per-function placement machinery = Scheduling Discipline + Closed Placement Surfaces), `era_hid_report_contract.md:269` (the 32 KiB floor), `era_overview.md:41` (placement never moves ownership), `era_performance_gates.md:402-403` (placement rules, image scope, budget), `era_active_index.md:105` ("why the vector table and the launch step are read together").
- **Dangling route into this document**: `era_invariants.md:243-246` says the mechanism for flash program/erase with core0 interrupts enabled and "the PRIMASK-to-NVIC trade hanging on it" are canonical here - the document contains no PRIMASK/NVIC-masking content (only `NVIC_SystemReset()` at 224). The nearest source is the comment at `system/era_vector_defaults.c:87-94` (HardFault/NMI not maskable by PRIMASK -> an erase fault used to vector into de-XIP'd flash). Add one row under Vector Table Residency or fix the route.
- Outbound routes it must keep valid: `era_board_adoption.md`'s **Copy-To-RAM Policy** (173, exists at adoption:94); Layout Gate / `.vectors` gate in `era_performance_gates.md` (186, 380-381; heading there is `## Layout Checks`, gates:400 - the prose form is unchecked; unify the name per A5).

**4. CHECKER TRAPS**
- No failing paragraph today. Paragraphs that pass only via a path elsewhere in the same paragraph: 78-157 (`early_hardware_init_pre()` relies on the `.mk`/`.ld` refs at 79-80); 280-285 (relies on `quantum/main.c:44`, verified = `keyboard_init();`); 287-304 (`era_split_transport_scheduler_init()`, `ensure_core1()`, `__init_ram_areas()` rely on the `.c` at 290 - `crt0_v6m.S:267-281` does not count); 313-326 (`era_nvm_replace()` relies on the NVM agent's uncommitted `storage/era_nvm.c`). Uppercase names (`NVIC_SystemReset()`, `chCoreGetStatusX()`) are not matched; `clocks_init()` (221) is not resolvable in the checker's roots.
- `path:line` citations that rot: 120 (`crt0_v6m.S:289`/`:306`), 283 (`quantum/main.c:44`), 300 (`crt0_v6m.S:267-281`); the `.S` ones resolve through the submodule-tail lookup and move on any ChibiOS bump.
- Dates: none.

**5. Duplication**
- Launch site / policy split 280-296 == `era_invariants.md:329-336` (MUST form), `era_walkthrough.md:213-217`, `era_source_map.md:164`.
- Carve-out selector rules 89-141 == `keyboards/era/ld/ERA_RP2040_SRAM_RESIDENT.ld:88-119, 286-330` comments (the document itself defers at 137-141); closure review == `era_performance_gates.md:410-423`; `era_board_adoption.md:112-113`.
- Vector defaults 169-178 == `era_source_map.md:171`, `era_board_adoption.md:176-184` (the five conditional slots), ld:312-330.
- Pre-copy window 102-109, 233-249 == `era_source_map.md:170` (768 x 240, MMIO-only), `era_invariants.md:187, 299-305`.
- Double-tap magic 195-231 == `era_invariants.md:203-212`, `era_performance_gates.md:413-419`, `era_qmk_fork_ledger.md:39`.
- Flash write guards 259-274 == storage 55-57, 87-90, 471-474; Apply-Liveness bullets 313-326 == storage 403-441; bank maintenance 328-337 == storage 464-485 == `era_invariants.md:347-353`.
- Gate description 365-376 == `common/tools/era_residency_gate.sh:37-44` comments and `era_performance_gates.md` **Layout Checks** (procedure owner).

### A6-c. Spot-check results (via `git grep`, worktree)

Matched: 24576 (`era_storage_adoption.h:69`); 128 KiB = 2 x 64 KiB, page 256, sector 4 KiB, `ERA_NVM_FORMAT_VERSION` 1 (`era_nvm_format.h:11-18`); chunk 16 (`era_nvm.c:12`); config 256 @ 37, portable 176, protected 176..255, baseline 220..251 = 32 (`era_eeprom_layout.h:36-142`); quiet 1000 (`era_host_peer_storage.h:56`); 500 (`ERA_STORAGE_QUIET_DEFER_MS` in board `post_rules.mk`, consumed at `quantum/eeconfig.h:201-216`); news mask 0x7F / pending 0x80 (`era_split_wire_protocol.h:277, 285`); `UINT16_MAX` (`era_host_peer_storage.h:164`); State Sync 0x06 / 0x01 / 0x00 / 0x07 and bytes 3, 4-5, 6, 8, 12, 16 (`era_state_sync.h:11-20`, `era_state_sync.c:204-233`); offsets 3 / 4 / 23, VIA layout 296 = 293 + 3, keymap 297 (`era_storage_layout.h:22-35`); macro 16384 and `IMAGE_BYTES` 16384; `program/erase_failure_count` (`era_nvm.h:100-109`); `REMOTE_APPLY`, `BUSY` (`era_nvm.h:22-39`); `EECONFIG_MAGIC_NUMBER_OFF` (`quantum/eeconfig.c:59, 70`). SRAM: ram4/5/7 origins (ld:36-39), 32768 of 262144 and 48 entries (`era_residency_gate.sh:72-74, 115`), 38 `ERA_VECTOR_DEFAULT`, 768 x 240 with `_Static_assert` == 184320 (`era_boot_core1_halt.c:100-112`), 512-word core1 stack and 48-entry 256-aligned table (`...lifecycle_rp2040.c:50, 123-126`), selectors ld:123-141, ASSERTs ld:288-362, magic section ld:237-242, `quantum/main.c:44`.

Not checked (build/ChibiOS-side or measured): 3 KB + 288 B core0 stacks, 16 B ram0 cost, ~1.6 KiB unclaimed, the three reserves, `CRT0_*` values, crt0 line numbers (submodule).

---
## A7. `manuals/era_board_adoption.md` (527 lines), `contracts/era_hid_report_contract.md` (404 lines), `contracts/era_closed_surface_contract.md` (273 lines)

Cross-cutting facts about the checker (read via `git show`): a "paragraph" is a blank-line-delimited block, so **a whole table is one paragraph** - one `.c/.h/.mk` token anywhere in it satisfies the function-claim arm for every row. Claim verbs are English only; a bare backticked `config.h` counts as a file ref; `.txt` does not. Route forms checked: `` `x.md`'s **H** ``, `` `x.md`, **H** ``, `` `x.md` **H** ``, ``(`x.md`, H)``; the target may be a heading **or any bold span**. Constant check: on one line, a resolvable `ERA_|RGB_MATRIX_|MATRIX_|RP2040_` name followed within 90 chars by integers must have the tree's value among them (`1000U` is invisible to it; `1000` is checked).

### A7-a. `era_board_adoption.md` - 527 lines

| Heading / block | Lines | N | Verdict | Survives | After |
| --- | --- | --- | --- | --- | --- |
| header + ownership pointer | 1-14 | 14 | K | header; "source map / sram contract / build options own the rest" | 8 |
| ## Storage Adoption | 16-50 | 35 | T 45 / K 25 / D 30 | 5-precondition table (32-38) verbatim; bundle = `storage/era_storage_adoption_rules.mk` from `post_rules.mk` above the split fragment, force-includes `era_storage_adoption.h` after `config.h`; refusal in `era_split_qmk_rules.mk:29`; "not gated on the sync selector" rule. Delete 18-23, 40-43 essays | 14 |
| ## Non-Split Capability | 52-92 | 41 | P 50 / K 30 / D 20 | eligibility rule (RP2040, `COL2ROW`, no own matrix source -> `CUSTOM_MATRIX`), manifest default `?= yes`; "split requires engine, engine requires nothing of split" (`era_split_qmk_rules.mk:202`); settle debt: `ERA_RP2040_MATRIX_GPIO_INPUT_PIN_DELAY` 128 measured on TOMAK79H only, every other board owes device first-run. Delete guard-shape story 54-64, "measured not argued" 76-82 -> one line | 12 |
| ## Copy-To-RAM Policy | 94-190 | 97 | mixed | see sub-rows | 40 |
| - policy | 96-105 | 10 | K | owner decision XIP retired; 22 RP2040 boards on it; `sirind/brick65` (atmega32u4) permanent exception, takes none of the layer | 4 |
| - bundle + gate | 107-118 | 12 | P | one line each: `system/era_sram_resident_rules.mk` is the include; `common/tools/era_residency_gate.sh` bands are `era_performance_gates.md`'s | 3 |
| - flash-safety premise | 120-127 | 8 | K | NVM program/erase runs while firmware + both vector tables execute from SRAM; <=1 4-KiB sector per housekeeping call; bank-rotation width = device-measurement obligation | 3 |
| - pre-copy arm inseparable | 129-140 | 12 | K 50 / P 50 | two linker checks (`early_hardware_init_pre` ASSERT, `.era_bootloader_magic` size asserts); failure = double tap dead silently | 3 |
| - double-tap window behaviour | 142-151 | 10 | P 70 / D 30 | one line (non-blocking window, only physical RUN arms, ROSC guard 15-102 ms) - owner is the sram contract | 2 |
| - three conditions | 153-172 | 20 | T | table: condition x what catches it (PRE_COPY_ARM needs NONBLOCKING; both without closer bricks; `$(error)` in `era_sram_resident_rules.mk` on missing `era_common_qmk_rules.mk`; no `RP2040_BOOTLOADER_DOUBLE_TAP_RESET` -> 2 linker ASSERTs; every board `_TIMEOUT 1000U`) | 8 |
| - conditional vector slots | 174-190 | 17 | T 80 / K 20 | table 180-184 verbatim (routed by `era_sram_residency_contract.md:173`); rule "answer with the driver's condition, never delete"; "run the `.vectors` gate when adopting" | 10 |
| ## Non-Split Board Baseline | 192-450 | 259 | mixed | see sub-rows | 70 |
| - what a non-split board gets | 194-197 | 4 | K | routed by `era_overview.md:27` | 2 |
| - PIO+DMA backend | 199-208 | 10 | P | claims PIO1 whole + 2 DMA channels; own PIO1 use = the one unsupported case | 2 |
| - VIA surface | 210-214 | 5 | P | one line; fix dangling pointer (see traps) | 1 |
| - SYNC split-only | 216-219 | 4 | K | 1 line | 1 |
| - USB suspend loop boundary | 220-235 | 16 | T 60 / K 40 | 2 rows: split = `NO_USB_STARTUP_CHECK = yes` (refused otherwise, `era_split_qmk_rules.mk:207`), deletes QMK loop (`tmk_core/protocol/chibios/chibios.c`), `era_split_keyboard.c` replaces; non-split = stock loop; n86 cflags evidence 1 line | 4 |
| - two detectors (owner decision) | 237-248 | 12 | K | decision + `system/era_usb_session.c` common; rejected split apply via `suspend_power_down_quantum()` -> REFUSED form | 4 |
| - one event seen twice | 249-257 | 9 | K | 2 lines - the detector model `era_authority_contract.md:71` routes here | 2 |
| - frame arm observed absence | 259-270 | 12 | K 50 / D 50 | rule 2 lines; delete EEPROM-lamp story (263-268) -> one grounding clause | 3 |
| - `USB->SOFRD` side effect | 272-281 | 10 | K | 2 lines: stand-off while `USB_INTE_DEV_SOF` set, 1 kHz rate limit (`era_source_map.md:172` routes the hardware fact here) | 2 |
| - sleep-gate owner | 283-303 | 21 | P 60 / D 40 | one row: resolver in `era_split_keyboard.c` writes the gate; `era_host_peer_response.c` publishes, `era_host_peer_source_snapshot.c` captures; two render overrides are not owners. Delete 295-303 meta | 3 |
| - ChibiOS deltas | 305-314 | 10 | T | 2 rows: SETUP-packet wake unconditional (`hal_usb_lld.c`); `RP_USB_SYNC_SUSPEND_AFTER_REMOTE_WAKEUP_SET` split-only (`split/era_split_usb_sleep_rules.mk`) | 3 |
| - unmeasured | 316-321 | 6 | K | 1 line: powered-hub host death -> `USB_SUSPENDED` unmeasured, owed | 1 |
| - lighting families | 322-336 | 15 | T 70 / D 30 | table family x count x VIA channel/id prefix; 4 two-family boards; `newone/h1` none; conversion = owner decision. Delete 332-336 meta | 6 |
| - RGB improvements RGB_MATRIX-only | 337-341 | 5 | K | 1 line / table column | 1 |
| - no-surface boards | 342-352 | 11 | K | owner decision 2026-08-18 (divine, klein_hs, klein_sd); `ERA_BACKLIGHT_ALWAYS_ON` repairs stored `enable = 0`; `BACKLIGHT_DEFAULT_LEVEL` in `config.h` | 3 |
| - two homes rule | 354-377 | 24 | T | table home x when x files x ids (`era_backlight_via.h` 0..3, `era_rgb_indicator_via.h` 6..12, `system/era_board_hooks.c` weak pair, two overriders); router-order rule 1 line. Delete 374-377 (identifier map owns) | 7 |
| - persistence gate rule | 379-396 | 18 | K 60 / T 30 / D 10 | 4-part rule; table of the three gates (channel x file); joining = `era_common_via_keyboard_channel_save()` (`system/era_common_via.c`). Delete 394-396 | 7 |
| - discrete controls | 398-403 | 6 | K | 1 line | 1 |
| - two set-time toggles | 405-415 | 11 | T | 2 rows (NKRO bit `features/era_nkro_via.c`; Velocikey `newone/common/odessey_common.c`; compare-first) | 3 |
| - strong render hooks | 417-423 | 7 | K | 1 line naming the four `_kb` hooks `era_rgb_indicator.c` defines | 2 |
| - n8x migrated definition | 424-434 | 11 | D 85 | rule only: "check a migrated definition against a handler" | 1 |
| - two-direction check + sweep | 436-450 | 15 | D 85 | rule only: every declared lighting hardware owes a surface or a decision; every menu entry owes a handler | 2 |
| ## The Adoption Checklist | 452-527 | 76 | mixed | | 30 |
| - checklist | 454-469 | 16 | K | 7 bullets, compressed | 10 |
| - board `.c` not required + skeletons | 471-488 | 18 | T 50 / D 50 | table class x six hooks (`system/era_nonsplit_board.c`, `split/era_split_board.c`, verified 6+6); "no non-split board keeps a `.c`" 1 line. Delete fave65s/brick65s/a1/et_tkl story | 6 |
| - closer hazard | 490-501 | 12 | K | `era_common_features_task()` (`system/era_common_features.c:132`) sole caller of `rp2040_bootloader_double_tap_reset_task()`; `ERA_BOARD_COMMON_ENABLE = no` re-acquires it | 3 |
| - VIA definition JSON | 503-517 | 15 | K 50 / D 50 | not compiled; owner-authored; app source `the-via-eerraa/era-definitions/custom/v3`, official `the-via/keyboards/v3`; no Custom-VIA-only controls in QMK-local files; one per half beside the `via` keymap (26 = 23 + 3) | 5 |
| - re-prove command | 519-527 | 9 | K | command + the two checks | 4 |

**Totals:** 527 -> ~176.

**MUST-SURVIVE.** REFUSED blocks: none exist; 245-247 ("routing the split apply through `suspend_power_down_quantum()` ... rejected on evidence") is a refusal in prose - give it the three-line form. Values: `TOTAL_EEPROM_BYTE_COUNT == 24576` / `WEAR_LEVELING_LOGICAL_SIZE` 24 KiB, backing x2; `DYNAMIC_KEYMAP_MACRO_EEPROM_SIZE` exactly 16 KiB; `EECONFIG_SIZE == 37`, `ERA_EEPROM_CONFIG_ADDR == 37U`; `VIA_EEPROM_MAGIC_ADDR == ERA_EEPROM_CONFIG_END`; layout-options 296 / keymap 297; `ERA_EEPROM_KEYBOARD_CONFIG_SIZE` 8; `ERA_RP2040_MATRIX_GPIO_INPUT_PIN_DELAY` 128; `RP2040_BOOTLOADER_DOUBLE_TAP_RESET_TIMEOUT` 1000U on all 22; ROSC guard 15-102 ms; families 10 / 11 / 5, two-family = klein_sd + classicd_a1_ug/core/coreless, h1 none (all verified); VIA channels 3/2/1; backlight ids 0..3, indicator ids 6..12 (verified in the `_via.h` files); PIO1 + 2 DMA; frame padded to next power of two of rows; 19 non-split boards with no `.c`; 26 VIA json; Vector5C/64, 50, 6C/70 with their three conditions. Owner decisions: XIP retired (96); two detectors (237); no lighting surface on divine/klein_hs/klein_sd (346); family conversion is an owner decision (329). Device facts: 128-cycle settle measured on TOMAK79H only (grounds per-board first-run debt); `slp == brkms` conviction (grounds the observed-absence clause); n86 cflags (grounds the loop boundary); `.vectors` gate caught the slot class three times (grounds "run it when adopting"); powered-hub `USB_SUSPENDED` **unmeasured, owed** (316-321). Headline rules: every RP2040 board copy-to-RAM; bundles are includes; storage adoption not gated on the sync selector; split => engine, engine =/=> split; `NO_USB_STARTUP_CHECK` split-only; frame arm counts only observed absence; SOFRD stand-off + 1 kHz; one sleep-gate writer; SETUP wake unconditional / re-suspend delta split-only; RGB improvements RGB_MATRIX-only; hardware owes a surface or a decision, menu entry owes a handler; two homes + common router first; persistence rule (one constant, arm = save never set, no max-age flush, one range one gate); discrete = immediate, two set-time toggles compare-first; common unit takes the four render hooks strongly; vector-slot condition rule; pre-copy arm inseparable; `ERA_BOARD_COMMON_ENABLE = no` re-acquires the hazard; VIA-JSON rules. Procedure: checklist bullets 456-469; `.vectors` gate on adoption (187-190); device first-run; `era-build era/newone/odessey60s:via` + decode-loop and `nm` checks (522-527).

**ROUTED HEADINGS (exact text must stay):** **Copy-To-RAM Policy** <- `era_overview.md:20`, `era_sram_residency_contract.md:173` (expects the five conditional vector entries here), `era_active_index.md:22`, `era_build_and_flash.md:65` (expects the brick65 exception), `era_source_map.md:212` (expects "include not variable"). **Non-Split Board Baseline** <- `era_authority_contract.md:71` (expects the detector model), `era_overview.md:27`, `era_active_index.md:90`; bare-bold from `era_source_map.md:172` (expects the SOFRD hardware fact). **Storage Adoption** <- bare-bold `era_build_options.md:319`; `era_source_map.md:118-122` declares the five preconditions canonical here. Prose names (unchecked but reader-bound): "lighting-surface rule" (`era_active_index.md:109`), "the adoption checklist" (`era_source_map.md:153`), "double-tap closer hazard" (`era_source_map.md:163`). File-level only: `era_feature_path.md:9,23`, `era_walkthrough.md:258`, `era_source_map.md:93`.

**CHECKER TRAPS.** Function-claim paragraphs (all currently carry a file token - keep one in whatever row/paragraph inherits the `foo()`): 54-64, 129-140, 142-151, 220-235, 237-248, 272-281, 283-293, 342-352 (passes only via the bare `config.h` loophole), 365-370, 379-396, 405-415, 471-488, 490-501. Dates: 346 keep (owner decision); 396 delete (story); 439 delete (sweep report); 484 delete (story). Dangling intra-doc pointer: 213-214 says "**The VIA surface and its gate** below" but that heading is `era_build_options.md:278` - write it as `` `era_build_options.md`'s **The VIA surface and its gate** `` (bare bold is unchecked, so it never failed). Constants: rows naming `ERA_EEPROM_KEYBOARD_CONFIG_SIZE`, `ERA_RP2040_MATRIX_GPIO_INPUT_PIN_DELAY`, `ERA_STORAGE_QUIET_DEFER_MS`, `ERA_VIA_*_VALUE_ID` must put the tree's integer (8, 128, 500, 0..3/5/6..12) within 90 chars after the name on the same line, or no integer at all. Ids are defined in `era_backlight_via.h`/`era_rgb_indicator_via.h`, not the `.[ch]` pair the doc names (359, 361).

**Duplication:** 142-151 (double-tap window, ROSC) -> `era_sram_residency_contract.md:197-242` (index's boot/reset row makes it canonical); 107-118 -> `era_source_map.md:212` + `era_performance_gates.md`; 237-257 -> `era_authority_contract.md:67-73` already states the two-detector predicate; 283-303 -> `era_authority_contract.md` **Lighting Sleep Ownership** (:75); 372-377 -> `era_identifier_map.md:113-115, 127-130`; 379-396 `ERA_STORAGE_QUIET_DEFER_MS` value/readers -> `era_build_options.md:198`, `era_qmk_fork_ledger.md:44`; 210-214 -> `era_build_options.md` **The VIA surface and its gate**; 25-45 mechanics -> `era_source_map.md:93`; 66-74 default -> `era_build_options.md` selector table; 503-517 workflow -> `docs/user/readme_split.txt`.

### A7-b. `era_hid_report_contract.md` - 404 lines

**Language:** not only `Read when:` - the title, `Canonical for:`, `Read when:` and the entire body are Korean; 13-14 says so ("only this document; owner instruction; translation needed"). The header check passes because the labels are English. **Restructure = translate to English + compress in one pass.** Header also says "세 건의 거절" (three refusals); the body holds **five** REFUSED blocks.

| Heading | Lines | N | Verdict | Survives | After |
| --- | --- | --- | --- | --- | --- |
| header | 1-14 | 14 | K (translate; drop 13-14 after) | | 6 |
| ## 결론 | 16-26 | 11 | K 40 / D 60 | decision: spec 6-key report + opt-in NKRO toggle; 1.7 became a contract | 3 |
| ### 1.1 report shape | 30-54 | 25 | T | table: `report_keyboard_t` = mods+reserved+`keys[6]`, 8 B, no report id (`KEYBOARD_SHARED_EP` undefined anywhere); own interface, boot subclass/protocol; `KEYBOARD_EPSIZE` 8; 1 ms poll (`USB_POLLING_INTERVAL_MS` default); REPORT_COUNT 6 x SIZE 8; no 8 kHz path. Delete H7S comparison | 8 |
| ### 1.2 boot/report protocol | 56-94 | 39 | T 50 / K 30 / D 20 | protocol-state table (initial REPORT; USB reset -> REPORT; SET/GET_PROTOCOL act only on `KEYBOARD_INTERFACE`; no SET_PROTOCOL -> stays REPORT); `send_keyboard()` sends 8 B in boot; GET_REPORT returns actual sent length; REFUSED 88-94 verbatim (translated). Delete "deviation 1/2" narrative | 14 |
| ### 1.3 NKRO path | 96-123 | 28 | T | table: `report_nkro_t` 32 B / 240 keys, `NKRO_REPORT_BITS` 30; shared endpoint `REPORT_ID_NKRO`; branch = `host_can_send_nkro() && keymap_config.nkro`; `host_can_send_nkro()` = protocol REPORT; `NKRO_DEFAULT_ON`/`FORCE_NKRO` undefined; `keymap_config` sizeof 2 regardless of `NKRO_ENABLE` | 9 |
| ### 1.4 no SET_PROTOCOL | 125-136 | 12 | K | rule: NKRO on + host that never sends SET_PROTOCOL + boot-only reader = no input at all; which hosts = owed measurement; user doc already warns | 3 |
| ### 1.5 boards + VIA | 138-158 | 21 | T 50 / K 30 / D 20 | 23 `features.nkro` true; 22 get the toggle (brick65 excluded; selector derived from `NKRO_ENABLE`, `era_common_qmk_rules.mk:257`); 25/26 VIA files carry the menu; id 5 `ERA_VIA_NKRO_ENABLE_VALUE_ID` on `id_custom_channel`; adapter owns only the two `clear_keyboard()` calls, no `id_custom_save` | 7 |
| ### 1.6 fork practice | 160-186 | 27 | D 80 / P 20 | pointer to `era_qmk_fork_ledger.md`; one line "`report.h` is identical to upstream (in no diff)" grounding REFUSED 349 | 2 |
| ### 1.7 KKUK pulse | 188-218 | 31 | K | rule 3 lines (snapshot both arrays, restore both, keys only, one `kkuk_pulse_state` survives the tick); REFUSED 206-212 verbatim; ship defaults: neither on | 12 |
| ### 1.8 split/storage | 220-233 | 14 | K | report never crosses the wire; `keymap_config` 2-byte sync domain (`split/era_host_peer_storage.h:52`) -> width change never changes schema | 3 |
| ## 2. option comparison | 235-280 | 46 | D 90 | delete table + RAM arithmetic; keep 1 line: `KEYBOARD_EPSIZE` bounds the send (`usb_driver.c:253` `osalDbgCheck`) so a wider array forces a descriptor change | 2 |
| ## 3. recommendation | 282-356 | 75 | K 35 / D 65 | three REFUSED blocks verbatim (322-327, 333-338, 349-356); four grounds one line each | 26 |
| ## 4. remaining work / verification | 358-381 | 24 | D | run report; 42-vs-41 task is stale | 0 |
| ## 5. owed measurements | 383-404 | 22 | K 60 / D 40 | table of 3 owed measurements (SET_PROTOCOL sample; extended-report boot compat; `usb.device_version` cache); item 4 delete | 6 |

**Totals:** 404 -> ~103.

**MUST-SURVIVE.** REFUSED (five, verbatim in meaning - translated to English with the rest of the document): 88 "delete the boot branch of `send_keyboard()` and always send the extended report"; 206 "the pulse snapshots and restores `keyboard_report` only"; 322 "ship NKRO on by default via `NKRO_DEFAULT_ON`"; 333 "widen the keyboard array and give up the boot-protocol truncation"; 349 "grow `KEYBOARD_REPORT_KEYS` and `KEYBOARD_EPSIZE` for ERA and detach the report width from upstream" (REOPENS: choose D', not D). Values (all verified): `KEYBOARD_REPORT_KEYS` 6; 8-byte report; `KEYBOARD_EPSIZE` 8; 1 ms; `NKRO_REPORT_BITS` 30 -> 32 B/240 keys; `NKRO_DEFAULT_ON` false/undefined, `FORCE_NKRO` undefined; sizeof `keymap_config` 2; 23/22/26/25; id 5; 3 sync boards (tomak, tomak79h, tomak79s); KKUK default `enable = 0`. Owner decisions: keep option A; Korean-by-instruction (13) - superseded by the translation decision of this campaign. Device facts: none - explicitly a source reading; three owed measurements (388-398). Headline rules: protocol state machine; boot truncation stays; NKRO only via shared EP under the two-AND condition; pulse restores the active format, keys not mods; adapter contract of `era_nkro_via.c`; report size never crosses the wire; width never changes schema; every core fork owes a ledger line. Procedure: device falsifier for 1.7 (NKRO on + KKUK on, hold >=2 keys, none lost) - one line if kept.

**ROUTED HEADINGS:** none by name. File-level only: `era_active_index.md:91` (its Locate column already sends the value id to `era_identifier_map.md` and core-file owes to `era_qmk_fork_ledger.md`). Numbered headings can be renamed freely; the header's own "(1.7)" is the only cross-reference.

**CHECKER TRAPS.** Today `check_claims` sees zero claim paragraphs here (Korean has no listed verb). After translation, paragraphs with `foo()` + a listed verb and **no `.c/.h/.mk` token**: 202-204 (`clear_keys()` clears / `get_mods_for_report()`); 214-218 (`era_kkuk_apply_defaults()` sets - names only `.txt`); 127-131 (`host_can_send_nkro()`, if "returns true" appears); 88-94 REFUSED block (`send_keyboard()`; a blockquote is its own paragraph). Paragraphs whose file token must survive compression: 72-78, 80-86, 100-110, 111-115, 116-119, 154-158, 190-200, 206-212, 222-227, 314-320. Stale citations (in range, so unflagged): 37 `era_split_wire_diagnostics.c:780` -> the `SHARED_EP_ENABLE` test is at `:798`; 261 `features/era_kkuk.c:59` is a blank line and the file holds no `report_keyboard_t` instance any more (snapshots are function-local `saved_keys`/`saved_bits`, `era_kkuk.c:163-176`); 167/371/399 "42 files vs ledger 41" - `git diff --name-only c93ef27143 HEAD -- tmk_core quantum platforms drivers builddefs` returns **32** today and the ledger states no count at all. 1.6's per-file table is still right (only `usb_descriptor.[ch]` are in the diff). Dates: none. Constant: write `ERA_VIA_NKRO_ENABLE_VALUE_ID` beside the 5 (the doc currently writes only "value id is 5").

**Duplication:** 1.6 -> `era_qmk_fork_ledger.md:10-20` (derivation) and its per-file rows; 1.5's "indicator slots start at 6 because of 5" -> `era_identifier_map.md:114, 129-130`; section 4 -> `era_performance_gates.md` **What A Change Owes** (:52); 1.8 -> `era_host_peer_matrix_contract.md` (raw matrix crosses) and `era_host_peer_storage_contract.md` (domains); 1.4 guidance -> `docs/user/readme.txt` NKRO item; section 2's 32 KiB floor -> `era_sram_residency_contract.md`.

### A7-c. `era_closed_surface_contract.md` - 273 lines

| Heading | Lines | N | Verdict | Survives | After |
| --- | --- | --- | --- | --- | --- |
| header | 1-8 | 8 | K | | 6 |
| ## Currently Open Exceptions | 10-180 | 171 | T 60 / K 25 / D 15 | lead 12-23 -> 3 lines (the `ERA_SPLIT_WIRE_SECTION_ELIGIBLE_*` table in `era_split_wire_protocol.h` enforces; every entry adds no op / class id / responder send; list exhaustive). 17 bullets -> one table: exception x relation/cell x marker/body x what it may never carry x owner contract. Keep verbatim-ish: ACTIVITY "no key identity, no matrix, nothing that enters the HID path" (76-78, cited by `era_wire_contract.md:671`); restart arm "carries an act, not a feature ... may never carry arbitrary work" (136-142, cited by `era_overview.md:158`); CLEAN PREPARE/COMMIT phases = one act (144-148); AUTHORITY flags byte last bit spent (167-169); 180. Delete "paid the Opening Rule" narratives (92-109 part, 110-119 part, 175-178), "closed the last core0 periodic frame" history | 55 |
| ## Closed Until Explicitly Opened | 182-228 | 47 | T 70 / K 30 | table closed item x enforcement/why; keep "each HOST-PEER closure compile-asserted beside its eligibility entry (`era_split_wire_protocol.h`)" (verified: asserts at :598-626), both **permanently** items, time-anchor seat zero | 24 |
| ## Storage Lane Boundary | 230-239 | 10 | K | all four bullets - **no verbatim owner found elsewhere** by grep (see duplication); keep as 4 one-liners | 5 |
| ## Classifier Rule | 241-244 | 4 | K | verbatim | 3 |
| ## Opening Rule | 246-273 | 28 | K | 5-item list verbatim; "naming a slice never opens"; two non-openings (carrier change, narrowing) one line each; "eligibility bytes are the proof" | 16 |

**Totals:** 273 -> ~111.

**MUST-SURVIVE.** REFUSED blocks: none in three-line form; the two `**permanently**` closures (`DUAL_RUNTIME_BUNDLE`/class `0x6x` 214-218; time-anchor seat 208-212) and 180 "do not authorize responder independent send" are refusals - three-line form optional (a permanent refusal may say so per AGENTS.md). Values (verified against `era_split_wire_protocol.h`): push markers `0x40` STORAGE_PENDING (1-byte body, `:185,:295`), `0x80` RESTART_ARM (5-byte body, `:208,:320`); response `STORAGE_NEWS` bits 0..6 value / bit7 pending (`:285`), response mask full (8 markers); AUTHORITY flags bits 3..7 restart intent, byte full (`:456` asserts 0xFF); CLEAN `PREPARE(param=1,T=0)` / `COMMIT(param=2,T!=0)`; `CHUNK_REQ` reserved bytes = 24-bit chunk-CRC hint, zero-length `CHUNK_RSP`; class `0x40` ERROR_NACK retired (`:117`), class `0x6x` absent (`:821`); `0x20`/`0x21` envelope pair. Owner decisions: none explicit. Device facts: none; device falsifiers owed for STORAGE_PENDING, the paired-rise, and the restart acts. Headline rules: table enforces / list exhaustive; one shape per entry; responder independent send closed by role; compile-asserted HOST-PEER closures; time-anchor seat permanently zero; storage lane boundary x4; classifier rule; Opening Rule x5; slice naming opens nothing; carrier change and narrowing are not openings; eligibility bytes are the proof.

**ROUTED HEADINGS:** none by name. File-level: `era_invariants.md:390` (expects the `EEPROM_SYNC` shape closure, 205-206), `era_overview.md:103` (expects "the enforceable form": eligibility entry, no own carrier), `era_overview.md:158` (expects "what the cell is open for", 136-142), `era_wire_contract.md:671` (REFUSED WHY cites "no key identity by design", 76-78), `era_feature_path.md:46` (expects "what opening costs" = **Opening Rule** - keep that heading text, it is also in `Canonical for:`), `era_source_map.md:442`, `era_active_index.md:88,92,93,98,101`, `.claude/rules/era-storage.md:14`, `era-wire.md:13`.

**CHECKER TRAPS.** No `foo()` anywhere; no `2026-` dates. Hex tokens (`0x40`, `0x80`, `0x6x`, `0x20`/`0x21`) are locatability-checked - keep them spelled as the tree spells them (`0x6x` resolves via `era_split_wire_protocol.h:821`). Outbound routes this doc must keep resolvable: `era_host_peer_storage_contract.md`'s **Diagnostics** (125) and `era_wire_contract.md` / `era_route_contract.md` / `era_authority_contract.md` / `era_identifier_map.md` / `era_performance_gates.md` / `era_split_wire_protocol.h` file mentions.

**Duplication:** bodies and markers (STORAGE_NEWS bit7, STORAGE_PENDING, RESTART_ARM, AUTHORITY bits, CHUNK_REQ hint, ACTIVITY body, the "three alternatives for another bit") -> `era_wire_contract.md` (this doc says so at 124, 148, 159, 168) - keep only cell/relation/never-carry here; retired kind numbering -> `era_identifier_map.md:76`; `SESSION_STATUS` cadence -> `era_route_contract.md`; gate lists inside "paid the Opening Rule" -> `era_performance_gates.md`. **Storage Lane Boundary** (232-239): `git grep` over `contracts/` finds "initiator selects storage", "not durable completion", "Core1 never reads live ... never writes EEPROM" and the Classifier Rule sentence **only in this file** - treat them as canonical here until `era_authority_contract.md`/`era_host_peer_storage_contract.md` are shown to hold them; do not delete on the assumption of duplication.

---

## A8. 나머지 9편 — 일반 규칙 적용 지침

감사하지 않았다. `DOCS_SYSTEM_PLAN.md` §8.1의 쓰기 규칙을 그대로 적용하되:

| 문서 | 줄 | 지침 |
| --- | --- | --- |
| `maps/era_source_map.md` | 450 | 이미 표. REFUSED 3 유지. 위 감사들이 "source map이 소유"로 지목한 행은 그대로 두고, 감사가 "이 문서가 사본"이라 지목한 서술(예: 185행의 절대값 재서술, 427-431의 in-place upgrade 순환 소유)만 정리. "commit gate" 문구는 Phase 1에서 이미 교체됨 |
| `maps/era_identifier_map.md` | 286 | 이미 표. RGB/INPUT arm 재서술(176-192)은 authority contract를 가리키는 사본 — 표 행은 남기고 설명 문장만 제거 |
| `maps/era_walkthrough.md` | 260 | **경로별 서사가 존재 이유** — 표 전환 없음. 각 단계에서 파일명과 한 줄 설명만 남기는 압축. 51행 `(era_route_contract.md, Due/Deadline Model)`, 183행 **What The Lane Costs A Typist**, 240행 **Relation Hold**, 24행 **Fixed Baselines** 라우팅은 대상 헤딩이 살아남으므로 유지 |
| `manuals/era_build_and_flash.md` | 243 | 절차 문서. 명령·기대 출력 유지, 배경 서사 삭제. `.era-artifacts/` 언급(89행)은 유지 |
| `contracts/era_overview.md` | 187 | 모델+용어집. 용어집은 이미 표 형태; 모델 서술은 한 문단씩 검토 |
| `era_active_index.md` | 136 | Phase 3의 행 추가 외 변경 없음 (매트릭스가 밀도의 기준) |
| `manuals/era_qmk_fork_ledger.md` | 127 | 표 + 유도. 유도 문단(10-20)은 규칙 근거이므로 유지 |
| `manuals/era_feature_path.md` | 121 | 순서가 가치 — 유지, 압축만 |
| `contracts/era_host_peer_matrix_contract.md` | 103 | 이미 짧음 — 일반 규칙 |
