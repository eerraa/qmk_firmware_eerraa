# Copyright 2026 Hyojin Bak (@eerraa)
# SPDX-License-Identifier: GPL-2.0-or-later

# Host-side proof of the PIO+DMA matrix sampler's hardware-free half
# (keyboards/era/common/system/era_rp2040_matrix_pio_frame.h): instruction
# encodings, the settle/release program, row patterns, decode tables against
# the header's reference rule, and the ring-frame arithmetic. Header-only subject,
# so no SRC line: the test translation unit includes it directly.
